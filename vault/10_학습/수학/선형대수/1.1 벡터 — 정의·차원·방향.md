---
유형: 개념원자
분야: 수학
주제: 선형대수 (벡터 — 정의·차원·방향)
관점:
  - 선형대수
  - 머신러닝
도서: "[실전선대] 1장 §1.1, §1.1.1 (벡터와 기본 연산)"
상태: 정리완료
이해도: 🟡
태그:
  - 수학/선형대수
  - 벡터
  - 개념원자
관련코스: "AI 암반 정복 1단계 — [[01 벡터 — 정의·차원·방향 (NumPy 생성·시각화)|렌즈]]"
---
# 벡터 — 정의·차원·방향

> 📐 **수학 개념원자(SSoT)** — 선형대수. 정의·성질·증명·기하 직관. AI 적용·연결은 코스 렌즈가 끌어다 쓴다.
> 🔗 [[_수학 MOC]] ｜ 🌐 [[_지식 연결 허브]] ｜ 🎓 AI 렌즈: [[01 벡터 — 정의·차원·방향 (NumPy 생성·시각화)|01(AI)]]

> [!info] 🔄 갱신 2026-08-05 — 외부 글(공돌이의 수학정리노트 「벡터의 기본 연산」) 융합: **벡터 정의의 세 층위**(화살표 / 수의 나열 / 벡터 공간의 원소)와 각 층의 강점·한계 표 신설 · **좌표계 변환 불변성**(벡터는 불변, 성분만 가변) 절 신설 + 원문 그림 1 병기 → 기저·좌표(1.8)로 가는 다리 연결. 공리 상세는 1.5로 위임(중복 서술 없음) (✅검증 — 원문 마크다운 전문 직접 대조 + 정규직교 기저 변환을 수식·수치로 독립 재확인: 성분 $(3,4)\to(3.649016,\,3.418286)$, 되조립 $(3.000000,\,4.000000)$, 노름 $5.0$ 보존)

> 🎯 **핵심 (TL;DR)**
> - **벡터** = 순서대로 나열된 수. (이 책은 *수 벡터*만 다룸)
> - **차원(dimensionality([ˌdɪmɛnʃəˈnælɪti], 디멘셔낼리티, 디멘셔낼리티))** = 원소 수. **방향(orientation([ˌɔːriɛnˈteɪʃən], 오리엔테이션, 오리엔테이션))** = 열(세로) vs 행(가로).
> - **`ℝ^N`** = N차원 실수 벡터 (ℝ([ɑːr], 알, 알)=실수, ℂ([siː], 시, 시)=복소수). 예: 원소 2개 → `ℝ²`.
> - ⚠️ **수학 차원 ≠ 파이썬 차원:** 수학 차원=원소 수 / 파이썬(NumPy) 차원=`ndim`(축 개수). 벡터의 원소 수는 파이썬에서 **length·shape([ʃeɪp], 셰이프, 셰이프)**.
> - **NumPy 4가지 생성:** 리스트 / 1차원 배열(방향 X) / 행벡터(1,N) / 열벡터(N,1). 방향은 `shape=(행,열)`로 드러남.
> - **기하 해석:** 벡터 = 크기(magnitude([ˈmæɡnɪtjuːd], 매그니튜드, 매그니튜드)) + 방향(angle([ˈæŋɡl], 앵글, 앵글)). 꼬리(시작)→머리(끝). 꼬리가 원점이면 **기준 위치(standard position([ˈstændərd pəˈzɪʃən], 스탠더드 퍼지션, 스탠더드 포지션))**.

> [!note] 🗺️ 이 노트 읽는 법
> [실전선대] 1장을 *척추*로 선형대수의 첫 대상인 **벡터**를 정식으로 판다. 0단계에서 [[02 텐서 이해하기|텐서]]를 "다차원 컨테이너"로 봤다면, 여기선 그 **1차원 사례인 벡터**를 선형대수 정식으로 다룬다. 수학 표기(ℝ^N)와 NumPy shape를 *나란히* 익히는 게 이 장의 핵심.

---

> [!question] 🎯 필요성 사슬 (결핍 → 필요 → 발명)
> *출발 문제:* 데이터(수의 묶음)를 수학적으로 다루려면, 먼저 그것을 담을 단일 대상이 필요하다. 그 모든 것의 1번 부품이 **벡터 자체**다.
> 1. 여러 수를 하나로 다뤄야 함 → 한 덩이로 묶을 형태 필요 → **벡터(순서 있는 수의 묶음)**
> 2. "정보가 몇 개냐(특징 수)"를 세야 함 → 원소 개수를 셀 척도 필요 → **차원(dimensionality)**
> 3. 같은 수라도 가로/세로에 따라 곱이 달라짐 → 데이터를 행/열로 세울 규약 필요 → **방향(orientation, 행/열)**
> 4. 칠판 수식과 코드의 '차원'이 어긋나 디버깅 지옥 → 둘을 나란히 볼 도구 필요 → **NumPy shape·ndim**
> **한 줄:** 수를 한 대상으로 묶음 → 벡터 → (개수=차원) + (행/열=방향) + (코드=shape)로 정식화.

> [!abstract] 📘 책 핵심 — [실전선대] §1.1 벡터 생성·시각화
> **① 벡터의 두 특징**
> - **차원(dimensionality):** 벡터가 가진 **원소의 수**.
> - **방향(orientation):** **열 방향**(세로로 세움) vs **행 방향**(가로로 누움).
> - 표기 **`ℝ^N`**: ℝ=실수, N=차원. 원소 2개 → `ℝ²`(R2, R^2로도 씀).
>
> **② 열벡터·행벡터 (식 1-1)**
> $$ x=\begin{bmatrix}1\\4\\5\\6\end{bmatrix},\quad y=\begin{bmatrix}.3\\-7\end{bmatrix},\quad z=\begin{bmatrix}1&4&5&6\end{bmatrix} $$
> - x = 4차원 **열**벡터 (`x∈ℝ⁴`), y = 2차원 열벡터, z = 4차원 **행**벡터.
> - **x와 z는 다른 벡터다.** 같은 순서·같은 원소라도 **방향이 다르면 엄밀히 다름**.
>
> **③ 칠판 수학 vs 코드 수학 (중요한 용어 충돌)**
> - **수학적 차원** = 원소 수. **파이썬 차원** = 출력에 쓰는 *기하학적 차원의 수*(NumPy `ndim`, 축 개수).
> - 모든 벡터는 NumPy에서 **2차원 배열**로 만들 수도, *방향 없는* 수 나열은 **1차원 배열**로도. 수학적 차원(원소 수)은 파이썬에선 **length / shape**.
> - 표기: 벡터 v = 소문자 진한 로마자 **v**(또는 이탤릭 *v*, 화살표 $\vec v$). 행벡터 = **$w^\mathsf{T}$** (T=전치 → [[02 텐서 이해하기|전치]]).
>
> > [!quote]- 📐 NOTE — 벡터 방향이 중요한가? (책)
> > 데이터를 *저장*할 때 방향은 보통 무관. 하지만 **어떤 파이썬 연산은 방향이 틀리면 예기치 않은 결과·오류**(브로드캐스팅 등). "행벡터를 열벡터로 바꿔야 함"을 몰라 30분 디버깅하는 일을 피하려면 방향을 의식해야 한다.
>
> **④ NumPy 생성 4가지 (코드)**
> ```python
> asList  = [1,2,3]                 # 리스트(선대 연산엔 부적합)
> asArray = np.array([1,2,3])       # 1차원 배열 — 방향 없음
> rowVec  = np.array([[1,2,3]])     # 행(1×3)
> colVec  = np.array([[1],[2],[3]]) # 열(3×1)
> ```
> - 방향은 **대괄호**로 지정: 바깥 대괄호=한 객체, 내부 괄호=행. `rowVec`=1행에 전부, `colVec`=숫자 1개짜리 행이 여럿.
> - **shape**: `asList (3,)` · `asArray (3,)` · `rowVec (1,3)` · `colVec (3,1)`. 차수 = **(행, 열)**.
> - 1차원 `asArray`는 `(3,)`(방향 없음), 방향 부여 벡터는 **2차원** `(1,3)`/`(3,1)`.
>
> **⑤ §1.1.1 기하학적 해석**
> - **대수적 해석:** 순서대로 나열된 수 목록.
> - **기하학적 해석:** 특정 **크기(magnitude·길이)** + **방향(angle, 양의 x축 기준)** 을 가진 직선. **꼬리**(시작)→**머리**(끝), 화살표.
> - 벡터 ≠ 좌표(점)지만, **꼬리가 원점에서 시작하면 일치** = **기준 위치(standard position)**.
> - 기하 해석 = 물리·공학(힘)에 유용 / 대수 해석 = 데이터과학(시간별 판매)에 유용.
>
> ![[fig-1-1 vectors-at-various-positions.png|460]]
> ▲ 그림 1-1 — 모든 화살표는 **동일한 벡터**(크기·방향 같음). 기준 위치 벡터는 꼬리가 원점, 머리가 기하학적 좌표를 가리킨다.

> [!abstract] 📘 책 핵심 — [필수수학] §4.1 도입 (벡터 정의 도판, 구 1.3에서 이관)
> [필수수학]도 같은 정의로 시작한다: 벡터 = *방향+길이*, 음수 성분 = 해당 축의 반대 방향. 용도: 물리(방향+크기)·수학(xy 평면 방향+스케일)·CS(숫자 배열) — 데이터 과학에선 *데이터의 한 조각*.

![[MD4-1-2 vector-basics-and-examples (figure).png|720]]

🔄 **재구성** [필수수학 그림 4-1·4-2] — 벡터 = 방향+길이. 음수 성분이면 해당 축의 반대 방향.

> [!quote]- 📖 책 원본 — [그림 4-1] 간단한 벡터 · [그림 4-2] 여러 예시
> ![[MD4-1 simple-vector (book-scan).png|240]]
> ![[MD4-2 various-vector-examples (book-scan).png|520]]

![[MD4-3 3d-vector (figure).png|420]]

🔄 **재구성** [필수수학 그림 4-3] — 3D 벡터 `[4,1,2]` 와 기저 `i, j, k`. (3D 좌표·기저 → [[3D 변환 행렬 — 동차좌표·회전·투영 (선형대수 응용)|3D 변환]])

↻ **인터랙티브** — 드래그로 회전(Plotly 플러그인 시; 미설치 시 위 PNG 폴백):

```plotly
data:
  - {type: scatter3d, mode: lines, x: [0, 4], y: [0, 1], z: [0, 2], line: {color: "#1c7ed6", width: 8}, name: "v=[4,1,2]"}
  - {type: scatter3d, mode: markers, x: [4], y: [1], z: [2], marker: {size: 4, color: "#1c7ed6"}, showlegend: false}
  - {type: scatter3d, mode: lines, x: [0, 1], y: [0, 0], z: [0, 0], line: {color: "#868e96", width: 4}, name: "i"}
  - {type: scatter3d, mode: lines, x: [0, 0], y: [0, 1], z: [0, 0], line: {color: "#2f9e44", width: 4}, name: "j"}
  - {type: scatter3d, mode: lines, x: [0, 0], y: [0, 0], z: [0, 1], line: {color: "#e8590c", width: 4}, name: "k"}
layout:
  margin: {l: 0, r: 0, t: 0, b: 0}
  scene: {aspectmode: cube}
```

> [!quote]- 📖 책 원본 — [그림 4-3] 3차원 벡터
> ![[MD4-3 3d-vector (book-scan).png|240]]

> 💭 **여기서 내 사고가 분기됨 ↓**

## 🧠 근본 이해 · 융합 · 통찰

### 🧩 '벡터'의 정의는 하나가 아니다 — 세 층위와 각 층의 한계
> 📥 **외부 글에서 가져온 틀** — 공돌이의 수학정리노트 「벡터의 기본 연산(상수배, 덧셈)」 도입부. 원문 전문 정리·수치 검증·정밀성 갭은 → [[벡터의 기본 연산 — 상수배·벡터 간의 합 (공돌이의 수학정리노트 정리)|공돌이 원문 정리]]. 여기엔 **틀과 각 층의 강점·한계만** 두고, 상세는 담당 원자로 넘긴다.

[실전선대]는 벡터를 **"순서대로 나열된 수"**(대수) 로 정의하고 기하 해석을 덧붙이는 순서로 간다. 그런데 "벡터란 무엇인가"의 답은 실은 **세 층위**로 갈리고, 층마다 강점과 **적용 범위의 한계**가 다르다. 셋은 경쟁하는 정의가 아니라 **같은 대상을 다른 높이에서 본 것**이다.

| 층위 | 정의 | 강점 | 한계 | 이 볼트의 담당 |
|---|---|---|---|---|
| ① **화살표** | 크기와 방향으로 정의되는 값 (물리학) | 기하 직관 · **좌표계 변환에 불변** | **유클리드 벡터**에만 적용 가능 | 위 📘 §1.1.1 기하학적 해석 |
| ② **수의 나열** | 순서를 맞춰 숫자를 나열한 리스트 | 데이터 포인트 관점 · **차원 확장이 공짜**(숫자만 더 붙이면 고차원) | 성분이 **좌표계에 따라 변함** — 숫자만으로는 "무엇을 어떤 자로 쟀는지" 알 수 없음 | 이 노트의 주 정의(TL;DR·📘 책 핵심) |
| ③ **벡터 공간의 원소** | $(V,+,\cdot)$ 의 원소 — 공리를 만족하면 벡터 | **가장 일반적** — 함수·행렬처럼 화살표로 못 그리는 것도 벡터로 다룸 | 순환적으로 보임(공간을 모르면 정의가 안 잡힘) · 시각화 불가 | → [[1.5 벡터 공리 — 덧셈·음수·역원\|1.5 벡터 공리]] |

- **①과 ②는 같은 그림의 두 축이다.** ①은 *벡터(불변)* 를, ②는 *성분(가변)* 을 각각 붙잡는다 — 다음 절이 이 한 문장의 근거다.
- **③이 "수학적으로 가장 의미 있는" 이유:** 이 정의를 택하면 함수·행렬·다항식처럼 화살표로 그릴 수 없는 대상에도 벡터의 기법(선형결합·투영·기저)을 그대로 적용할 수 있다. ⚠️ 단 "덧셈·상수배에 닫혀 있으면 벡터 공간"은 **필요조건일 뿐**이다 — 공리가 8개 더 필요하다. 항등원·역원·$(-1)v=-v$ 를 포함한 정확한 내용은 [[1.5 벡터 공리 — 덧셈·음수·역원|1.5]]가 주인이므로 **여기서 반복하지 않는다.**

> [!note] ✅ 이미 이 노트가 다룬 것 — "수학의 벡터는 시점이 원점"
> 같은 외부 글은 "물리학 벡터와 수학 벡터는 다르다 — 수학에서는 모든 벡터의 **시점이 원점**이어야 한다"고 덧붙인다. 이 노트는 위 📘 §1.1.1에서 같은 사실을 **기준 위치(standard position)** 라는 *명명 규약*으로 이미 서술했다. 차이는 어조뿐 — 원문은 **당위**("이어야 한다"), 이 노트는 **규약**("꼬리를 원점에 두면 머리 좌표 = 성분"). 새로 적을 것 없음.

### 🔁 성분은 좌표계에 상대적이다 — 좌표계가 변해도 벡터는 안 변한다
①의 진짜 값어치는 "화살표로 그리면 예쁘다"가 아니라 **불변성**(invariance([ɪnˈveəriəns], 인베어리언스, 인베리언스))이다: **좌표계를 바꿔도 벡터 그 자체는 가만히 있고, 바뀌는 것은 그 벡터를 적는 성분뿐이다.**

![[벡터기본연산-src-fig1 coordinate-change-invariance (ext-src).png|420]]
*원문 그림 1 — 좌표계의 변환과 벡터. 좌표계가 변할 때 벡터는 변하지 않지만 벡터의 성분은 변한다. 출처: 공돌이의 수학정리노트(Angelo's Math Notes), 「벡터의 기본 연산(상수배, 덧셈)」, CC BY-NC 4.0 — [원문](https://angeloyeo.github.io/2020/09/07/basic_vector_operation.html).*

$$ \vec v \;=\; 3\hat i+4\hat j \;=\; 3.649016\,\hat i_{new}+3.418286\,\hat j_{new} $$

성분은 $[3,\,4]^{\mathsf{T}}\to[3.649016,\ 3.418286]^{\mathsf{T}}$ 로 **바뀌었는데 화살표는 하나**다. 정규직교 기저끼리의 변환이면 이유가 한 줄로 끝난다 — 새 기저를 열로 갖는 회전 행렬을 $R$ 이라 하면 새 성분은 $c=R^{\mathsf{T}}v$ 이고, 그 성분을 새 기저로 되조립하면

$$ Rc=RR^{\mathsf{T}}v=Iv=v $$

**되조립하면 정확히 원래 벡터**다. (직접 계산 확인: $\theta=10^\circ$ 에서 되조립 $[3.000000,\ 4.000000]^{\mathsf{T}}$, 노름도 $5$ 그대로. 그림의 새 좌표계가 약 $10^\circ$ 반시계 회전이라는 역산·픽셀 계측은 → [[벡터의 기본 연산 — 상수배·벡터 간의 합 (공돌이의 수학정리노트 정리)|원문 정리 §🔍 검증 1]].)

**그래서 이 노트에서 무엇이 바뀌나.** 지금까지 이 노트는 벡터를 **원소 수(차원)** 와 **행/열(방향)** 로만 봤다. 여기에 한 층이 더 붙는다 — 적혀 있는 그 숫자들이 이미 **"어떤 자로 쟀나(기저)"에 상대적**이라는 것. `[1,2,3]` 이나 `[3,4]` 는 벡터의 *정체*가 아니라 **표준기저로 읽은 값**이다.

- ⚠️ **혼동 주의 — 움직이는 건 좌표계지 벡터가 아니다.** 벡터에 회전 행렬을 곱해 *벡터 자체를 돌리는* 것은 정반대 연산이고, 그때는 벡터가 진짜로 변한다(→ [[2.10 회전·기하 변환 — 순수 회전·직교성·전단|2.10 회전]]). 여기서는 벡터를 고정한 채 **읽는 자만** 갈아 끼웠다.
- ⚠️ **①의 한계 재확인** — 이 불변성 논지는 **유클리드 벡터**(Euclidean vector([juːˈklɪdiən], 유클리디언, 유클리디안))에 한해서다. 함수 공간의 '벡터'에는 화살표 길이·각도를 그대로 붙일 수 없다 → 그래서 ③이 필요하다.
- 🌉 **다리 — 여기가 기저(1.8)의 씨앗이다.** "성분은 기저에 상대적"이 자라면 [[1.8 벡터 확장 — 집합·선형결합·선형독립·부분공간·생성·기저|1.8]]의 *"데이터는 안 변한다 — 보는 자(기저)가 분석을 정한다"* 가 되고, 거기서 **최적 기저 찾기 = PCA·차원축소·특징추출**로 이어진다.
- 🔗 **0단계 텐서와 같은 문장이다.** [[02 텐서 이해하기|텐서]]가 "텐서 = 좌표계가 바뀌어도 안 변하는 본질"이라 했던 그 말의 **벡터(1차 텐서) 판**이 바로 이것 — 바로 아래 절과 이어서 읽을 것.

### 🔗 0단계 '텐서'와의 연결 — 벡터 = 1차 텐서
[[02 텐서 이해하기|텐서]]에서 "스칼라(0차)·벡터(1차)·행렬(2차)·…"로 봤던 그 **벡터(1차 텐서)** 를 선형대수로 정식화한 것. 0단계는 *컨테이너·shape* 관점, 여기선 *수학적 대상(차원·방향·기하)* 관점. 같은 객체의 두 렌즈.

### ⚠️ 가장 헷갈리는 지점 — '차원'이라는 한 단어, 두 의미
| | 수학적 차원 | 파이썬 차원(`ndim`) |
|---|---|---|
| 뜻 | **원소 수** | **축(axis) 개수** |
| `[1,2,3]` | 3차원 벡터 | `ndim=1` (1차원 배열) |
| `[[1,2,3]]` | 3차원 (행)벡터 | `ndim=2` (2차원 배열, shape (1,3)) |

→ "3차원 벡터"가 NumPy에선 1차원 배열일 수 있다. **수학 차원=원소 수=`size`/`len`, 파이썬 차원=`ndim`.** 이 충돌을 모르면 shape 디버깅에서 헤맨다.

> [!tip] 🌱 탐구 · [[T04 NumPy 벡터 내부 — shape·strides·열벡터 (N,1)|NumPy 벡터 내부 — 1D 블록·strides·(N,1)]]
> `(3,)`(방향 없음) vs `(3,1)`(열) vs `(1,3)`(행)이 *왜* 다른 객체인지 — ndarray=1D 연속 블록+`shape`/`strides`(=C 포인터 산술)라는 내부 정체와, `.T`·`@`·`np.linalg`가 1D에서 깨지는 이유. **두 책의 열벡터 표기 차이의 근원.** ↳ [[_탐구 MOC]]

### 🧭 방향이 '엄밀히 다른' 이유 — 행/열은 곱셈 유효성을 가른다
x(열)와 z(행)가 원소가 같아도 다른 건 단순 규약이 아니다. **행렬 곱은 (행×열) 차수가 맞아야** 성립하므로, `(1,3)`이냐 `(3,1)`이냐가 *어떤 곱이 가능한지*를 결정한다. → 내적/외적·`Wx+b`에서 방향이 결과를 좌우(다음 절·[[3D 변환 행렬 — 동차좌표·회전·투영 (선형대수 응용)|행렬곱 차수]]).

> [!quote] 🌉 타 영역 연결 — 벡터 = '공간의 점 또는 방향'   `#연결/수학`
> 기하 해석의 *기준 위치(꼬리=원점)* 벡터는 곧 **공간의 한 점(좌표)**. [[3D 변환 행렬 — 동차좌표·회전·투영 (선형대수 응용)|3D 변환 노트]]의 `w=1`(점) / `w=0`(방향) 구분이 바로 이 "벡터=점 vs 방향"의 동차좌표판. 그래픽스(공간)·데이터과학(특징)·신경망(`Wx+b`)이 *같은 벡터·행렬*을 공유. 전역 다리 → [[_지식 연결 허브]]

## 🧩 코드 실습 (§1.1)
> 🎯 목표: 벡터를 4가지로 생성해 **방향이 shape에 어떻게 드러나는지** 확인하고, 같은 벡터를 여러 위치에 그려 **기준 위치** 개념을 시각화.
> 코드: `~/LOCAL/notebooks-mirror/ai-practice/01_linear_algebra/la1_vectors.py` (검증: shape `(3,)·(3,)·(1,3)·(3,1)`)

```python
import numpy as np
asList  = [1,2,3]
asArray = np.array([1,2,3])       # (3,)   방향 없음(ndim=1)
rowVec  = np.array([[1,2,3]])     # (1,3)  행
colVec  = np.array([[1],[2],[3]]) # (3,1)  열
print(asArray.shape, rowVec.shape, colVec.shape)
```

![[vector-geometry-same-vector-multiple-positions.png|460]]
▲ **내가 직접 그린 코드 출력**(`la1_vectors.py`) — 같은 벡터 `v=[-1,2]`를 세 위치에. 모두 동일 벡터, 꼬리가 원점인 것이 **기준 위치**. (위 📘 책 핵심의 그림 1-1을 NumPy/matplotlib로 재현해 본 것)

관찰 포인트:
- **`asArray`(1차원)는 방향 없음** — 행도 열도 아닌 순수 수 나열. 방향이 필요하면 `[[...]]`로 2차원화.
- **대괄호 깊이 = 방향** — 내부 괄호가 '행'을 만든다. `(1,3)` vs `(3,1)`.
- **벡터는 위치 무관** — 꼬리를 어디 두든 크기·방향 같으면 같은 벡터. 좌표와 일치하는 건 *기준 위치*뿐.

## 🆕 새 용어 정리
| 용어 | 한 줄 정의 |
|---|---|
| **벡터** (Vector) | 순서대로 나열된 수 |
| **차원** (Dimensionality, 수학) | 벡터의 원소 수 (≠ 파이썬 `ndim`) |
| **방향** (Orientation) | 열(세로) vs 행(가로) |
| **`ℝ^N`** | N차원 실수 벡터 공간 (ℂ=복소수) |
| **열벡터 / 행벡터** | 세로(N,1) / 가로(1,N); 전치($w^\mathsf{T}$)로 변환 |
| **shape / length** | (행,열) 차수 / 원소 수 = 수학적 차원 |
| **`ndim`** (파이썬 차원) | NumPy 배열의 축 개수(방향 없는 벡터=1) |
| **크기** (Magnitude) | 벡터의 길이 |
| **기준 위치** (Standard Position) | 꼬리가 원점에 있는 벡터(좌표와 일치) |
| **불변성** (Invariance) | 좌표계를 바꿔도 대상 자체는 변하지 않는 성질 — 벡터는 불변, 성분은 가변(not invariant) |
| **유클리드 벡터** (Euclidean Vector) | '크기와 방향'으로 정의되는 벡터 — 화살표 정의가 통하는 범위(함수·행렬 같은 일반 벡터엔 그대로 적용되지 않음) |

## 📌 암기 카드
> 앞면(cue)을 보고 뒷면을 떠올린 뒤 확인. (→ [[_핵심 암기장]])

| 앞면 (cue) | 뒷면 (외울 내용) |
|---|---|
| 벡터 정의? | 순서대로 나열된 수 |
| 수학적 차원 = ? | 원소 수 (= `size`/`len`) |
| 파이썬 차원(`ndim`) = ? | 축(axis) 개수 (방향 없는 벡터=1) |
| `ℝ^N` 뜻? | N차원 실수 벡터 (ℝ=실수, ℂ=복소수) |
| 방향(orientation) = ? | 열(세로, N,1) vs 행(가로, 1,N) |
| shape `(3,)` vs `(1,3)` vs `(3,1)`? | 방향 없음 / 행벡터 / 열벡터 (= (행,열)) |
| 같은 원소·같은 순서인데 행≠열? | 방향이 다르면 엄밀히 다른 벡터 (x≠z) |
| 기준 위치(standard position) = ? | 꼬리가 원점에 있는 벡터 (머리 좌표 = 성분) |
| 행벡터 표기? | $w^\mathsf{T}$ (T=전치) |
| 벡터 정의의 세 층위? | ① 화살표(크기·방향) ② 순서 있는 수의 나열 ③ 벡터 공간의 원소(가장 일반적) |
| 좌표계를 바꾸면 무엇이 변하나? | **성분만** 변한다 — 벡터 자체는 불변(invariance) |
| ①(화살표) 정의의 한계는? | 유클리드 벡터에만 적용됨 (함수·행렬까지 담으려면 ③ 필요) |
| `[3,4]` 는 벡터의 정체인가? | 아니다 — **표준기저로 읽은 값**. 자(기저)를 바꾸면 숫자도 바뀐다 |

## 연결
- 🎓 AI 렌즈(적용·임베딩): [[01 벡터 — 정의·차원·방향 (NumPy 생성·시각화)|01 벡터(AI)]]
- 다음: [[02 벡터 크기와 단위벡터 (노름)|벡터 크기·단위벡터(노름)]] · 토대: [[02 텐서 이해하기|텐서(벡터=1차 텐서)]]
- 벡터=점/방향: [[3D 변환 행렬 — 동차좌표·회전·투영 (선형대수 응용)|3D 변환]]
- 📥 **외부 원문(정의 세 층위·불변성의 출처):** [[벡터의 기본 연산 — 상수배·벡터 간의 합 (공돌이의 수학정리노트 정리)|공돌이의 수학정리노트 — 벡터의 기본 연산]] `참고-출처정리`
- 정의 ③(벡터 공간의 원소)·공리: [[1.5 벡터 공리 — 덧셈·음수·역원|1.5 벡터 공리]] ｜ 성분↔기저의 상대성이 자라는 곳: [[1.8 벡터 확장 — 집합·선형결합·선형독립·부분공간·생성·기저|1.8 벡터 확장(기저)]]
- 영역: [[_수학 MOC]] ｜ 🌐 [[_지식 연결 허브]]

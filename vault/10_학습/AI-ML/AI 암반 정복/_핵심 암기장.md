---
유형: 마스터
코스: AI 암반 정복
태그:
  - meta
  - 암기
---

# 🧠 핵심 암기장 (마스터)

> **용어집의 쌍둥이.** 전 노트의 `## 📌 암기 카드`를 한곳에 모은 *암기 전용* 색인. **앞면(cue)을 보고 뒷면을 떠올린 뒤** 펼쳐 확인하는 자가 테스트용.
> 📖 정의 검색은 [[_용어집]] ｜ 🔙 [[_커리큘럼 MOC]] (규칙 15) ｜ 🌐 [[_지식 연결 허브]]
>
> **운영 규칙:** 노트에 암기 카드를 추가/수정하면 *같은 작업에서* 이 파일도 갱신(누락=버그). 새 항목은 해당 단계·영역 표에 한 줄 추가하고 출처 노트를 링크.

---

## 1단계 — 선형대수와 임베딩 (현재 진행)

### [[01 벡터 — 정의·차원·방향 (NumPy 생성·시각화)|01 벡터 — 정의·차원·방향]]
| 앞면 | 뒷면 |
|---|---|
| 벡터 정의? | 순서대로 나열된 수 |
| 수학적 차원 = ? | 원소 수 (= `size`/`len`) |
| 파이썬 차원(`ndim`) = ? | 축(axis) 개수 (방향 없는 벡터=1) |
| `ℝ^N` 뜻? | N차원 실수 벡터 (ℝ=실수, ℂ=복소수) |
| 방향(orientation) = ? | 열(세로, N,1) vs 행(가로, 1,N) |
| shape `(3,)` vs `(1,3)` vs `(3,1)`? | 방향 없음 / 행벡터 / 열벡터 (= (행,열)) |
| 같은 원소·순서인데 행≠열? | 방향이 다르면 엄밀히 다른 벡터 |
| 기준 위치(standard position)? | 꼬리가 원점 (머리 좌표 = 성분) |

### [[02 벡터 크기와 단위벡터 (노름)|02 노름·단위벡터]]
| 앞면 | 뒷면 |
|---|---|
| 노름 ‖v‖ 공식? | √(Σvᵢ²) = √(vᵀv) (원소 제곱합의 제곱근 = 유클리드 거리) = **거대해진 피타고라스**(→ 벡터 한 뿌리) |
| 노름 = 무엇의 거리? | 꼬리(초기점)→머리(종점) 거리 |
| 제곱 크기 ‖v‖²? | 제곱근 생략 (sqrt 비싸서 비교·최적화에) |
| 단위벡터 정의? | 크기가 1인 벡터 (방향만 의미) |
| 정규화(단위벡터 만들기)? | v̂ = (1/‖v‖)·v |
| 영벡터에 단위벡터? | 없음 — ‖0‖=0 → 1/0 불가 |
| `len()` vs `np.linalg.norm()`? | 차원(원소 수) vs 크기(길이) |
| 크기×방향 분해? | v = ‖v‖·v̂ |
| 노름 = 재정의? 계산? | 계산(구하는 행위) — 벡터 정의(성분)엔 크기 필드 없음 |
| 같은 (3,4), 노름 다 다름? | L1=7 / L2=5 / L∞=4 — 크기는 규칙의 결과, 고정 속성 아님 |
| 노름에서 벡터 복원? | 불가 — 다대일. 노름=5인 벡터=반지름 5인 원·구 전체(무한) |
| 거리 vs 노름? | d(x,y)=‖x−y‖ — 노름은 벡터 1개, 거리는 점 2개 |
| L1·L2·L∞ 한 줄? | 맨해튼(Σ\|vᵢ\|) / 유클리드(√Σvᵢ²) / 체비셰프(max\|vᵢ\|) |
| 노름 순서(같은 벡터)? | ‖x‖₁ ≥ ‖x‖₂ ≥ ‖x‖∞ (등호는 0 아닌 성분 ≤1개일 때만) |
| 단위구 포함관계? | L1⊆L2⊆L∞ (마름모 ⊂ 원 ⊂ 정사각형) — 잣대가 헐거울수록 도형 큼 |
| "L1·L2"의 L 유래? | 앙리 르베그(적분 이론 헌정) — 공간 실제 형식화는 리스(Riesz, 1910) |
| Lᵖ vs ℓᵖ 차이? | 대문자=연속(함수·적분) / 소문자=이산(벡터·수열) — ML은 관행상 대문자 통일 |

### [[03 벡터 연산 — 덧셈·뺄셈·스케일링·전치·브로드캐스팅|03 벡터 연산(통합 — 덧셈·뺄셈·스케일링·전치·브로드캐스팅)]]
| 앞면 | 뒷면 |
|---|---|
| 벡터 덧셈 = ? | 머리-꼬리 연결 (원소별 합); 같은 차원만 |
| 교환법칙? | v+w = w+v (순서 무관) |
| 뺄셈 v−w 기하? | 두 머리를 잇는 선 (w 머리 → v 머리) |
| 스케일링 = ? | 스칼라 곱으로 길이 조절 (각 원소에 곱) |
| 음수 스칼라 → ? | 방향 반전 — 단 같은 선상 (선형 종속의 씨앗 → 06) |
| 열벡터 + 행벡터 → ? | 브로드캐스팅으로 행렬 ((3,1)+(1,3)→(3,3)) |
| 전치(transpose) = ? | 행↔열 뒤집기 (mᵀᵢⱼ=mⱼᵢ); vᵀᵀ=v |
| `XᵀX` = ? | 대칭 공분산 행렬 (PCA의 뿌리) |
| 스칼라-벡터 덧셈? | 수학 불가(별도 객체) / 파이썬은 브로드캐스팅 |
| 벡터 평균 = ? | 다 더하고 1/N 곱 (= 데이터 평균) |
| 자명한 해(trivial)? | 영벡터 해 — 보통 비자명(0 아닌) 해를 찾음 |

### [[04 벡터 내적 — 점곱·기하·직교 + 아다마르곱·외적|04 내적·아다마르·외적]]
| 앞면 | 뒷면 |
|---|---|
| 내적 정의(대수)? | δ=Σaᵢbᵢ — 대응 원소 곱의 합 (같은 차원만, 결과=스칼라) |
| 내적 기하 정의? | δ=cosθ·‖v‖‖w‖ |
| 내적 부호 = 각도? | 예각 + · 직각 0 · 둔각 − |
| 직교 ⇔ ? | 내적 0 ⇔ 사잇각 90° (3자 동치, 양방향) 📌 |
| 코사인 유사도? | cosθ = (v·w)/(‖v‖‖w‖) — 내적을 크기로 정규화 → 방향만 |
| 내적 큼 ⇒ 각도 작음? | 틀림 — 부호만 각도가 정함, 크기는 두 노름이 좌우 |
| 내적 = 값이 비슷한가? | 아님 — 부호 정렬(방향)×크기 ([10,10]·[−10,−10]=−200) |
| 분배 법칙? | aᵀ(b+c)=aᵀb+aᵀc |
| aᵀb = bᵀa ? | 맞음 (둘 다 스칼라). aᵀb ≠ abᵀ (외적=행렬) |
| 왜 전치하나? | 내적을 행렬곱으로: (1×N)(N×1)=스칼라; a@b.T는 N×N→OOM |
| 아다마르곱 a⊙b? | 원소별 곱 (차원 유지); `a*b` |
| 외적 vwᵀ? | 열×행 → rank-1 행렬 (SVD·LoRA의 벽돌); `np.outer` |
| 외적은 '경우의 수'? | 아니다(용어 오류) — 경우의 수=개수 세기 / 외적=곱 값(상호작용 강도) 지도 Mᵢⱼ=vᵢwⱼ; grad ΔW=δxᵀ |
| Outer vs Cross Product? | Outer abᵀ=임의차원 행렬(AI) / Cross a×b=3D 한정 벡터(물리); 둘 다 한국어 "외적" |
| 곱 종류 구분(출력)? | 내적→스칼라 / 아다마르→벡터 / 외적→행렬 / 교차곱→3D벡터 / 행렬×벡터→벡터(변환) / 행렬×행렬→행렬(합성). 한국어 외적=Outer≠Cross (→용어집 곱 지도) |
| 내적 vs 외적 차원? | vᵀw=(1,1) 스칼라 / vwᵀ=(N,M) 행렬 |

### [[05 직교벡터 분해 — 투영·수직·평행 성분|05 직교벡터 분해(투영·수직·평행)]]
| 앞면 | 뒷면 |
|---|---|
| 직교 투영 계수 β? | β=aᵀb/aᵀa (식 1-12) |
| β 유도의 핵심 조건? | b−βa 가 βa와 직교(내적 0)일 때 거리 최소 |
| 평행 성분 공식? | t∥r=r·(tᵀr/rᵀr) (식 1-13) |
| 수직 성분 공식? | t⊥r=t−t∥r |
| 분해 검증 2가지? | ① 합이 원래 벡터 ② 두 성분 내적=0(직교) |
| β·평행성분이 미정의되는 경우? | 기준벡터가 영벡터일 때(aᵀa=0 또는 rᵀr=0) |
| 왜 "직교+평행"인가? | 이 조합만 유일한 분해를 줌(다른 방향 조합은 무한히 많음) |
| 이 절이 예고하는 곳? | 그람-슈미트·QR(8장), 최소제곱법(10장) |

### [[06 벡터 확장 — 집합·선형결합·선형독립·부분공간·생성·기저|06 벡터 확장(선형독립·기저)]]
| 앞면 | 뒷면 |
|---|---|
| 선형 가중 결합? | w=λ₁v₁+…+λₙvₙ (계수 λ로 가중해 더함); 같은 차원만 |
| 가중치 다르게 = 변조? | 아니다 — 원본 보존, 새 조합 w 생성; 다르게=중요도 반영(균등도 하나의 가정); 선형이라 구조 보존 |
| 선형 독립 vs 종속? | 한 벡터도 나머지 결합으로 표현 불가(독립) / 가능=중복(종속) |
| 선형종속 정식 정의(식2-3)? | 0=Σλᵢvᵢ에 비자명해(λ≠0) 존재 ⇔ 종속 |
| 영벡터 포함 집합은? | 항상 선형 종속 |
| 독립성은 무엇의 속성? | 개별 벡터가 아니라 집합의 속성 |
| 생성 vs 부분공간? | 생성=모든 선형결합(동사) / 부분공간=닫힌 결과(명사, 원점 포함) |
| 차원 = ? | 생성하는 독립 벡터 수 (= rank) |
| 기저의 두 조건? | ① 생성 ② 선형 독립 (→ 좌표 유일) |
| 기저는 유일? | 아니다 — 무한; 최적 기저 찾기=PCA·차원축소 |
| 기저가 왜 중요? | 데이터 불변, 관점(자)만 바꿔 적은 축에 핵심 담음 → 모든 차원축소·특징기법=최적 기저 찾기 |
| rank로 독립 판별? | rank=벡터수 →독립 / rank<벡터수 →종속 |
| 스팬(Span) 정의? | 스케일링+덧셈으로 도달 가능한 전체 공간 (=생성, 필수수학 표기) |
| 선형 독립 ⇔ 스팬? | 방향이 달라 스팬이 차원을 꽉 채움(평면 전체) |
| 선형 종속 ⇔ 스팬? | 같은 선/평면에 갇혀 스팬 축소(차원 붕괴) |
| "기울기 다르다=독립"은 항상 맞나? 📌 | **2D에서만** — 3D+에선 필요조건일 뿐, 진짜 기준=새 벡터가 기존 span을 벗어나는가(a,b,c=a+b는 방향 다 달라도 종속) |

### [[07 상관관계와 코사인 유사도 — RAG 검색엔진|07 상관관계·코사인 유사도(RAG)]]
| 앞면 | 뒷면 |
|---|---|
| 피어슨 상관계수 식3-1? | ρ=Σ(xᵢ−x̄)(yᵢ−ȳ) / [√Σ(xᵢ−x̄)²·√Σ(yᵢ−ȳ)²] |
| 상관계수 선대 표기(식3-2)? | ρ=x̃ᵀỹ/(‖x̃‖‖ỹ‖), x̃=x−x̄ (평균중심화) |
| 상관계수 = 무엇의 코사인? | **평균중심화된 벡터들의 코사인 유사도** |
| 두 정규화 순서? | ① 평균중심화(위치 제거) → ② 분할정규화(크기 제거) |
| ρ vs cos θ 차이 한 줄? | ρ=평균 뺀 뒤 각도(경향) / cosθ=원본 그대로 각도(절대 위치 포함) |
| 검증 예제 수치? | x=[0,1,2,3], y=[100,101,102,103] → ρ≈1, cosθ≈0.808 |
| 왜 ρ=1인가(이 예제)? | y=x+100은 순수 이동 → 평균중심화하면 x̃=ỹ(동일 벡터) |
| ρ가 [−1,1]에 갇히는 근거? | 코시-슈바르츠 부등식 \|uᵀv\|≤‖u‖‖v‖ — 코사인 성질을 상속 |
| 상관계수 = 정규화된 무엇? | 정규화된 **공분산** — ρ=Cov(x,y)/(σₓσᵧ) |
| RAG가 상관계수 아닌 코사인 쓰는 이유? | 쿼리·문서는 인덱스로 짝지어진 시계열이 아니라 독립된 점 — 뺄 "짝지은 평균"이 없음 |

### [[08 벡터 응용 — 시계열 필터링과 특징 탐지|08 시계열 필터링·특징탐지]]
| 앞면 | 뒷면 |
|---|---|
| 필터링 메커니즘? | 커널과 신호 조각(커널과 같은 길이)의 내적을 구하고, 커널을 한 칸씩 밀며 반복 |
| 필터링 = 무슨 기법? | 본질적으로 특징 탐지(feature detection) 기법 |
| 합성곱의 정확한 정의? | $(f*g)[n]=\sum_m f[m]\,g[n-m]$ — 커널을 **뒤집어서** 슬라이딩 |
| 상관관계의 정의? | $(f\star g)[n]=\sum_m f[m]\,g[n+m]$ — 커널을 뒤집지 **않고** 슬라이딩 |
| 합성곱=상관관계가 되는 조건? | 커널이 좌우 대칭일 때(뒤집어도 안 변함) |
| 이 책·CNN이 "합성곱"이라 부르는 것의 실체는? | 대부분 상관관계(관행적 명명) |
| 필터링된 신호 출력 길이("valid" 모드)? | 원신호 길이 − 커널 길이 + 1 |
| 필터링된 신호 값이 크다 = ? | 그 위치가 커널이 찾는 패턴과 닮았다(특징 존재) |

### [[09 k-평균 클러스터링 — 거리 기반 비지도 학습|09 k-평균 클러스터링]]
| 앞면 | 뒷면 |
|---|---|
| k-평균 5단계? | ①중심 초기화 ②거리 계산 ③최근접 배정(argmin) ④중심 갱신(평균) ⑤반복 |
| 거리 계산에 이중 for문 대신? | 브로드캐스팅 — `(150,2)-(1,2)`를 자동 150회 반복해 처리 |
| 코드에서 제곱근이 빠진 이유? | 거리·거리제곱은 단조 관계 → argmin 결과 불변 → 생략(→ T08) |
| `np.argmin` vs `np.min`? | argmin=최솟값의 인덱스(위치) / min=최솟값 자체(값) |
| 중심 갱신 방법? | 해당 클러스터에 배정된 모든 점의 평균(특징별) |
| k는 데이터가 알려주나? | 아니다 — 사람이 정하는 하이퍼파라미터(최적값 결정은 어려운 문제) |
| 비지도인 이유? | 라벨 없이 거리만으로 그룹을 스스로 찾음 |

### [[12 행렬 곱셈·행렬식 — 변환 합성·면적 배율·선형종속|12 행렬 곱셈·행렬식]]
| 앞면 | 뒷면 |
|---|---|
| 행렬 곱의 의미? | 변환 합성 — AB="B 먼저, 그다음 A"(안→밖) |
| 행렬곱은 왜 이상하게 정의? | 연립방정식 Ax=b 분리 표현; 곱 조건(앞 열수=뒤 행수)=미지수 수=식 수 |
| 행렬곱 두 시점? | 칸=내적(Σaₖbₖ) / 열=A 열들의 선형 가중 결합 — 둘 다 선형결합 한 원자 |
| Ax=b 두 그림(Strang)? | row=직선들의 교점이 해 / column=b를 만드는 A열 결합 계수가 해 |
| "가중치"는 LLM 용어? | 아니다 — 선형결합 계수=가중평균=회귀계수=Wx 한 뿌리; 새로움은 *학습*뿐 |
| 영인자(Zero divisor)? | A,B≠O인데 AB=O 가능; 모두 det=0(특이); 실수와 달리 소거법칙 안 됨 |
| AB = BA? | 아니다 — 비교환(순서가 결과를 바꿈) |
| 행렬곱 연산자? | `@`/`matmul`(3D+에선 dot과 다름); ⭐`*`는 아다마르(→11) |
| 행렬곱 유효성(내부/외부)? 📌 | (M×N)(N×K): **내부 N 일치=유효, 외부 M×K=결과** |
| "표준" 행렬곱의 '표준'은 왜? | 아다마르곱·스칼라곱과 구별 (표기: AB 무기호 vs A⊙B) |
| 행렬-벡터 곱 유효 형태? | Av(열벡터 뒤)·vᵀA(행벡터 앞); 결과 방향=곱한 벡터 방향 |
| 행렬-벡터 곱 두 해석? | 열들의 선형 가중 결합 / 기하 변환(회전+확대) |
| Mv가 방향 유지·배율만이면? | v=고유벡터, 배율=고윳값 (12장 예고) |
| 행렬식의 기하 의미? | 변환의 면적(부피) 배율 |
| 전단·회전의 det? | 1 (넓이 불변) |
| det 부호? | 음수 = 공간 방향 뒤집힘(반전) |
| det = 0 ⇔ ? | 차원 붕괴 = 선형 종속 = rank 부족 = 역행렬 없음(비가역) |

### [[14 역행렬·연립방정식 — 특수 행렬·AX=B 풀이|14 역행렬·연립방정식]]
| 앞면 | 뒷면 |
|---|---|
| 역행렬의 의미? | 변환 되돌리기(undo); 나눗셈 없는 행렬의 ÷ 역할 |
| 역행렬 정의식? | A⁻¹A = I (항등) |
| 역행렬 존재 조건? | det(A) ≠ 0 (정방·가역); det=0이면 없음 |
| 항등 행렬 I? | 대각=1·나머지=0; ×1=변환 취소; 원래 기저 |
| 연립방정식 행렬 표현? | AX=B (계수 A·미지수 X·상수 B) |
| AX=B 의 해? | X = A⁻¹B (양변에 A⁻¹ 곱해 A 취소) |
| 대각/삼각/희소? | 대각=축별 스칼라 / 삼각=풀기 쉬움(LU) / 희소=0 안 저장(메모리) |
| sympy 행렬곱 연산자? | `*` (numpy는 `@`); sympy=정밀 분수 |
| 단위 행렬 vs 항등 행렬? | 같은 identity matrix — 번역만 다름([실전선대]/[필수수학]); 단위벡터의 unit과 무관 |
| I₅ 뜻? | 5×5 단위(항등) 행렬 — 크기는 아래 첨자, 생략 시 문맥 추론 |
| 비정방 행렬 이름? | 행>열=**높다**(tall) / 열>행=**넓다**(wide) |
| np.diag의 이중 동작? | 행렬 입력→대각 벡터 추출 / 벡터 입력→대각 행렬 생성 |
| 대각 원소 추출 = 대각화? | 아니다 — 대각화는 별개 연산(12장 예고) |
| 상/하삼각 추출 함수? | `np.triu()` / `np.tril()` |
| 난수 행렬 생성? | `np.random.randn(행, 열)` — 가우스; 임의 크기·rank 실험용 |
| 영 행렬 표기? | 굵은 **0** — 영벡터와 같은 기호(중의적 표기는 관례); `np.zeros()` |

### [[10 행렬 — 정의·생성·시각화·인덱싱·슬라이싱|10 행렬 — 정의·인덱싱·슬라이싱]]
| 앞면 | 뒷면 |
|---|---|
| 행렬 표기? | 굵은 대문자 (𝐀, 𝐌) |
| 행렬 크기 규칙? | **(행, 열)** — 행 먼저 열 나중; 3×5 = 행 3·열 5 |
| a₃,₄ 뜻? | 3행 4열 원소 (수학 1-기반) |
| a₃,₄ 를 파이썬으로? | `A[2,3]` — 0-기반이라 각 인덱스 −1 |
| 파이썬 슬라이스 stop은? | **미포함**(half-open) — 길이 = stop−start; `A[1:4]` = 행 2~4(1-기반) |
| 수십억 원소 행렬은 어떻게 보나? | 이미지 시각화(원소↔색); 색 매핑은 임의 → 패턴만 읽는다 |
| 행렬의 세 가지 읽기? | 열벡터 나열 / 행벡터 쌓기 / 원소 격자 |
| 열 추출 시 (N,1) 유지법? | `A[:, [j]]` 또는 `A[:, j:j+1]` (`A[:, j]`는 1D `(N,)`) |

### [[11 행렬 연산 — 덧셈·뺄셈·이동·스칼라곱·아다마르곱|11 행렬 연산 — 덧셈·뺄셈·이동·스칼라곱·아다마르곱]]
| 앞면 | 뒷면 |
|---|---|
| 행렬 덧셈 방법·조건? | 대응 원소끼리; **크기가 같아야만** 성립 |
| λ + A 가능? | 수학적 불가 — `A+s`는 브로드캐스팅(선대 연산 아님) |
| 행렬 이동 정의? | A+λI — 단위 행렬에 λ 곱해 더함(정방 전용) |
| 이동하면 뭐가 변하나? | **대각 원소만** +λ, 나머지 그대로 |
| 파이썬 이동 코드? | `A + s*np.eye(len(A))` (`A+s`는 이동 아님!) |
| 왜 '적은 양'을 이동? | 수치 안정성 ↑ + 행렬 정보 최대 보존 |
| 이동량 λ의 기준? | 행렬 자체 값(노름·고윳값 평균)의 일부 (예고) |
| 이동의 두 응용? | 고윳값 찾기 · 정규화(릿지 XᵀX+λI) |
| 아다마르곱 A⊙B? | **원소별 곱** cᵢⱼ=aᵢⱼbᵢⱼ — 같은 크기만; 결과 크기 그대로 |
| ⭐ `A*B` vs `A@B`? | `*`=아다마르(원소별) / `@`=표준 행렬곱(행·열 내적) — 전혀 다른 연산 |
| MATLAB `*` 함정? | MATLAB `*`=행렬곱, `.*`=아다마르 — 파이썬과 정반대 |
| 아다마르 vs 행렬곱 크기? | ⊙: M×N⊙M×N→M×N / 행렬곱: M×N·N×K→M×K |
| 아다마르곱이 자주 쓰이는 이유? | 많은 개별 곱셈을 한 행렬에 **저장**(마스킹·게이트) |

### [[13 행렬 전치 — 내적·외적 표기법·LIVE EVIL·대칭 행렬|13 행렬 전치 — 표기법·LIVE EVIL·대칭 행렬]]
| 앞면 | 뒷면 |
|---|---|
| 전치 정의식(식 4-2)? | aᵀᵢⱼ=aⱼᵢ — 행↔열 교환 |
| Cᵀᵀ = ? | C — 이중 전치는 원래 행렬 |
| 1D 배열에 `.T` 하면? | **아무 일도 없음** — 경고·오류 없이 그대로 (그래서 명시적 (N,1)) |
| 내적을 aᵀb로 쓰는 이유? | 유효성 규칙: (1×M)(M×1) → 내부 일치·외부 1×1=스칼라 |
| 외적 abᵀ 크기? | (M×1)(1×N) → **M×N** 행렬 |
| (AB)ᵀ = ? 📌 | BᵀAᵀ — 각각 전치 + 순서 반전 (LIVE EVIL) |
| AᵀBᵀ는 왜 안 되나? | 내부 차원 불일치로 무효 — 유효성 규칙이 순서 반전을 강제 |
| LIVE EVIL 적용 범위? | 임의 개수 행렬 곱; 곱 전치의 **유일무이한** 방법 |
| 역행렬도 순서 반전? | (AB)⁻¹=B⁻¹A⁻¹ — 같은 계열(옷 입기/벗기 역순) |
| 대칭 행렬 정의? 📌 | Aᵀ=A (행=대응 열); **정방만 가능** |
| 비정방이 대칭 불가인 이유? | M×N의 전치는 N×M — 크기부터 달라짐 |
| 아무 행렬로 대칭 만들기? | AᵀA 또는 AAᵀ (곱셈 기법) — 항상 정방 대칭 |
| (AᵀA)ᵀ 증명? | =AᵀAᵀᵀ=AᵀA — LIVE EVIL + 이중 전치 |
| AᵀA = AAᵀ? 📌 | **아니다** — 둘 다 정방 대칭이지만 다른 행렬(비정방이면 N×N vs M×M) |
| AᵀA가 핵심인 곳? | 공분산 XᵀX·릿지 +λI·PCA — 통계 모델·특잇값 분해 |

### [[15 행렬 노름·대각합 — 프로베니우스·ℓ2·tr(AᵀA)|15 행렬 노름·대각합 — 프로베니우스·ℓ2·tr(AᵀA)]]
| 앞면 | 뒷면 |
|---|---|
| 행렬 노름은 하나? | **아니다** — 여러 개; 각각 행렬을 특징짓는 한 숫자; 표기 ‖A‖ |
| 행렬 노름 두 계열? | **원소별**(원소 크기) vs **유도**(변환의 크기 조정 배율, 6·13장 예고) |
| 프로베니우스 공식? | ‖A‖F=√(ΣᵢΣⱼa²ᵢⱼ) — 전 원소 제곱합의 제곱근(첨자 F) |
| 프로베니우스의 정체? | 행렬을 펴서(vectorize) 벡터 ℓ2 — 새 공식 아님(뿌리: 거대해진 피타고라스 → 벡터 한 뿌리) |
| ℓ2 별명의 유래? | 원소별 p-노름 (ΣΣ\|aᵢⱼ\|^p)^(1/p) 의 p=2 |
| 행렬 거리? | ‖A−B‖F — 식 5-1에 C=A−B 대입; 같으면 0 |
| 정규화의 기본 생각? | 노름을 비용 함수에 추가 — 가중치 크기 벌금 |
| L2 vs L1? | L2=릿지(비대 방지) / L1=라쏘(희소 방지) |
| 이동량 λ 기준(회수)? | ‖A‖F 등 행렬 자체 값의 일부 — 스케일 비례 |
| 대각합 tr(A) 정의·조건? | 대각 원소의 합; **정방 행렬만** |
| tr(A) = 무엇의 합(예고)? | 고윳값의 합 (고유공간 '부피' 측정치) |
| ‖A‖F 를 tr로? 📌 | √tr(AᵀA) — AᵀA 대각=각 열의 자기 내적(제곱합) |
| √tr(AᵀA)로 계산하면? | 개념은 아름답지만 낭비 — 실전은 `(A**2).sum()` |
| '대각'=주대각선(i=j) 하나뿐인 이유? | ① 자기→자기 성분(yᵢ 중 aᵢᵢxᵢ 직행분) ② 기저 불변 — tr(P⁻¹AP)=tr(A) |
| 반대각선(i+j=N+1)에 이론이 없는 이유? 📌 | 표기 순서의 우연 — 축 재번호에 흩어짐; 불변량 없음(교환행렬 J 등 이름만) |
| 프로베니우스 = "대각 구하기"? 📌 | 아니다 — 정의는 전 원소(식 5-1); √tr(AᵀA)의 대각은 AᵀA의 대각 = 열별 제곱합 계좌(전 원소 재포장); 비정방 A엔 tr(A) 자체가 없음 |

### [[16 행렬 공간 — 열공간·행공간·영공간|16 행렬 공간 — 열공간·행공간·영공간]]
| 앞면 | 뒷면 |
|---|---|
| 행렬 공간이란? | 행렬의 특징들 사이의 **선형 가중 결합** — 1.8 생성의 행렬판 |
| 열공간 C(A) 정의? | 열벡터 집합의 모든 선형 가중 결합(무한 실수 스칼라) = span(열들) |
| 열 1개의 열공간 모양? | 그 열 방향으로 원점 통과하는 무한 직선(1D 부분공간) |
| v∈C(A) 의 뜻? | v를 열들의 가중 결합으로 만들 수 있다 ⇔ Ax=v 해 존재 |
| N개 열 = N차원 열공간? 📌 | **아니다** — 차원 = 선형 **독립**인 열의 수 |
| R³ 속 독립 2열의 열공간? | R³에 포함된 2D 평면 — "우주를 가로지르는 얇은 종이" |
| 선형 결합 ≠ 평균? 📌 | 그렇다 — 실제 메커니즘은 **순차 이동(꼬리 물기)**; "평균처럼 보임"은 λ₁=λ₂=1 특수 결과만 본 착시 |
| 열공간=ℝ² 증명(대수)? | λ₁[1,3]+λ₂[1,2]=(a,b) 풀면 λ₁=b−2a,λ₂=3a−b — 항상 유일해 ⇔ det≠0(가역) |
| 부분공간 = 열공간? 📌 | **아니다** — 범주(부분공간) ⊃ 사례(열공간). 행공간·영공간도 부분공간의 다른 사례 — 동의어 아님 |
| 영공간 형성 조건 = "방향이 반대"? 📌 | **아니다** — 조건은 "기울기가 같음(=종속)" 하나뿐. 같은 방향이어도(예3: c₂=+2c₁) 영공간 생김 — 방향은 y 부호만 바꿈 |
| 소속 판별의 정량 도구? | 행렬 계수 rank (장 후반 예고) |
| 소속 판별이 여는 문? | **선형 최소제곱법의 시작**(10장) — b∉C(A)면 최선의 근사 |
| C(v)=λv 의 λ 신분? | 전 실수를 훑는 **자유 변수**(집합 생성) — 세 신분: 계수(1.3)·이동량(2.1 고정 상수)·고윳값(🔭) |
| C(v)=λv vs Av=λv? 📌 | 전자=v의 배수 **전부**(집합); 후자=고윳값 식 — λ는 **행렬이 정해주는 특정 상수**(🔭) |
| 행공간 R(A) 정의·표기? | 행들의 모든 선형 가중 결합 = C(Aᵀ) — 전치 하나로 열공간 이론 재사용 |
| R(A)=C(A) 인 행렬은? | **대칭 행렬**(Aᵀ=A) — 행 이야기=열 이야기 |
| 영공간 N(A) 정의? 📌 | Ay=0 을 만족하는 **0 아닌** y 들의 집합(자명해 y=0 제외) |
| 왜 y=0 은 제외? | 모든 행렬에서 성립하는 뻔한 답 — 정보 0(비자명해만 질문) |
| 독립 열 ↔ 영공간? 📌 | 열이 선형 독립 ⇔ N(A)={}; 최대(열)계수=빈·축소계수=비어 있지 않음 |
| 영공간의 정체(1.8 언어)? | 식 2-3(종속 정의)의 해집합 — 종속 증거(λ들)의 보관소 |
| 영공간은 어디와 직교? | **행공간** — Ay=0 = 각 행 aᵢ와의 내적 전부 0 (그림 5-4) |
| 영공간이 중요한 진짜 이유? | 고유벡터·특이벡터 찾기의 핵심(12장) — (A−λI)v=0: 고유벡터=**이동된 행렬의 영공간** |
| null_space 가 0.7071을 주는 이유? | 무한 후보 중 **단위벡터**(√½) 반환 — 알고리즘은 단위 기저 벡터가 관행 |
| 네 부분공간? | 열 C(A) · 행 R(A)=C(Aᵀ) · 영 N(A) · **왼쪽 영 N(Aᵀ)**(행의 영공간) |

### [[17 행렬 계수(rank) — 정보 차원·최대계수·이동·확장|17 행렬 계수(rank)]]
| 앞면 | 뒷면 |
|---|---|
| 계수(rank)는 어떤 숫자? 📌 | 행렬이 담은 **실질 정보 축의 개수** — 독립 열(=행) 최대 수 = 열공간 차원; 행렬당 정수 하나 |
| '계수'와 지수·coefficient 관계? 📌 | **무관 — 동음이의어**: rank=階數 / coefficient=係數(1.8 λᵢ) / exponent=指數 |
| 계수의 상한과 full rank? | r≤min{M,N}; 등호면 **최대계수**, 미만이면 축소계수(특이) |
| 열 rank와 행 rank? | **항상 같다**(비정방 포함) — 공간은 달라도 차원 수 동일(그림 5-5); 증명은 SVD(🔭13) |
| 스칼라곱과 계수? | 불변 — 단 0 곱은 예외(영행렬 → r=0) |
| rank(A+B)·rank(AB) 규칙? | ≤ rA+rB / ≤ min{rA,rB} — 개별 계수론 **최대치만** 안다 |
| 외적이 항상 계수-1인 이유? | (M×1)(1×N) — **중간 병목이 1**; rank(AB)≤min 의 특수 사례 |
| 이동 A+λI 와 계수? 📌 | 보통 **최대계수화**(r<M→M) — 정보 거의 불변(ρ≈0.99997)·가역 획득(릿지 4면) |
| 컴퓨터의 rank 계산 방식? | **추정** — svd 후 tol보다 큰 특잇값 개수(잡음의 가짜 축 필터) |
| 확장(augmenting) 소속 판별? 📌 | rank(A)=rank(A⊔v) ⇔ v∈C(A)(중복) / 커지면 ∉(새 정보) |
| 벡터 집합 독립성 판별? | 행렬에 넣고 r=N 독립 / r<N 종속 (책의 "r<M"은 오기 의심) |
| 종속의 세 표현? | r<N ⇔ N(A)≠{} ⇔ 어떤 열=다른 열들의 결합 (rank·영공간·정의) |
| "대각 0아닌 개수=rank"는 삼각행렬에도? 📌 | 아니다 — 대각행렬 전용. 삼각행렬은 대각 밖 원소가 독립 방향 추가 가능(반례 T=[[0,1,0],[0,0,0],[0,0,5]]: 대각 0아닌 1개, 실제 r=2) |

### [[T04 NumPy 벡터 내부 — shape·strides·열벡터 (N,1)|탐구 T04 — NumPy 내부]]
| 앞면 | 뒷면 |
|---|---|
| ndarray 정체? | 1D 연속 메모리 블록 + (dtype·shape·strides) 메타데이터 |
| shape vs strides? | shape=차원 형태 / strides=다음 축 원소로 가는 바이트 간격 |
| strides가 대신하는 C 산술? | arr[row*COLS+col] 포인터 산술 |
| (N,) vs (N,1) vs (1,N)? | 축 없는 1D ≠ 2D 열벡터 ≠ 2D 행벡터 |
| (N,).T 결과? | 그대로 (N,) — .T 먹통 |
| (N,)@(N,) 결과? | 행렬곱 아닌 내적(스칼라) |
| np.linalg에 1D? | LinAlgError — 최소 2D 요구 |
| 노트 열벡터 표기? | 명시적 2D (N,1); 변환 `reshape(-1,1)` |
| `.T`는 연속 메모리? | 아님 — strides만 바꾸는 비연속 view; 물리정렬은 `.contiguous()` |

### [[1.5 벡터 공리 — 덧셈·음수·역원|1.5 벡터 공리 — 덧셈·음수·역원 (원자)]]
| 앞면 | 뒷면 |
|---|---|
| 방향은 저장값인가? | 아니다 — 성분 비율의 파생값 (각도 = atan(y/x)) |
| 벡터 덧셈은 어떻게? | 성분별 독립 합(중첩 원리), 축끼리 간섭 없음 |
| 스칼라 차원? | 0차원(0차 텐서), 1차원 벡터(ℝ¹)와 구별 |
| 항등원: 덧셈/곱셈? | 덧셈 0 / 곱셈 1 |
| 덧셈 역원 정의? | x를 항등원 0으로 되돌리는 짝(−x); 음수의 본질 |
| (−1)·v = ? 근거? | −v; 직관 아닌 벡터 공간 공리에서 강제 |

### [[T06 벡터=상태 vs 행렬=연산자 (기저 재정의·SIMD·GEMM)|탐구 T06 — 상태 vs 연산자]]
| 앞면 | 뒷면 |
|---|---|
| 벡터 vs 행렬 본질? | 벡터=상태(공간 속 한 점) / 행렬=선형 사상(연산자, 함수) |
| Av는 무엇과 동일? | f(x) — 행렬은 입력→출력 벡터 함수 |
| 행렬의 각 열(column)? | 변환 후 기저축이 도착하는 위치 (기저 재정의) |
| 벡터/행렬 하드웨어 병목? | 벡터=대역폭 한계 / GEMM=연산 한계 |
| 병목 가르는 지표? | 산술 밀도 (FLOP/byte) |
| SIMD vs SIMT? | CPU=SIMD(AVX) / GPU=SIMT |
| "행렬=연산자" 전부 맞나? | 반만 — W=연산자, 데이터/공분산/이미지/인접행렬=데이터 컨테이너 |
| Av의 두 계산 순회? | 행별(내적) / 열별 누적(gaxpy) — 원자 2.2와 동일 시점 |
| gaxpy=GPU 텐서 코어 실제 방식? | 아니다 — CPU/BLAS 계산 순서일 뿐; 텐서 코어=타일 병렬 FMA |
| 배치(AX)로 묶어도 함수 정체성 안 바뀌는 근거? | 선형성 — AX=A[x₁…xₖ]=[Ax₁…Axₖ] |
| 실무가 배치로 묶는 진짜 이유? | 수학 아님 — GEMV(대역폭한계)→GEMM(연산한계) 전환, GPU 안 놀리려고 |
| 배치 전체에 행렬 놈의 역할? | 안전선 — ‖Axᵢ‖≤‖A‖‖xᵢ‖, 배치 크기 무관하게 성립 |

### [[T08 유클리드 거리 — 두 점 사이 거리·제곱거리의 순위 불변성|탐구 T08 — 유클리드 거리·모노토닉]]
| 앞면 | 뒷면 |
|---|---|
| 두 점 사이 유클리드 거리? | δ(p,q)=‖p−q‖=√Σ(pᵢ−qᵢ)² — 노름의 일반화 |
| 왜 코드에서 제곱근을 생략해도 되나? | sqrt는 순증가함수 → a<b ⟺ √a<√b → argmin(순위)이 안 바뀜 |
| "당연한 논증"의 숨은 전제는? | 반드시 **순증가(순감소)함수**일 때만 성립 — 임의의 변환은 안 됨(예: sin은 순위 안 보존) |
| 책의 결론 문장? | "거리와 거리 제곱은 단조로운 관계 → 결과는 같음 → sqrt는 코드 복잡성·시간만 늘림 → 생략 권장" |
| 이 논증이 쓰이는 곳? | k-평균(argmin 클러스터 배정)·k-최근접 이웃(🔭)·이상 탐지(🔭) |

### [[T09 확률은 왜 0.0~1.0인가 — 공리·곱셈 닫힘·부동소수점 팩트체크·로그 공간|탐구 T09 — 확률 0-1·로그 공간]]
| 앞면 | 뒷면 |
|---|---|
| 확률이 0~1인 **진짜** 이유 셋? | ① 정의가 비율(부분÷전체) ② 콜모고로프 공리 P(Ω)=1 ③ 곱셈 닫힘(p·q가 다시 확률; 0~100은 50×50=2500 이탈) |
| 콜모고로프 3공리? | 비음성 P(A)≥0 · 정규화 P(Ω)=1 · 서로소 가산 가법성 |
| float에서 크기가 커지면 정밀도는? | **상대** 정밀도는 일정(ε≈2.22e-16, 지수·가수 분리) — 커지는 건 **절대** 간격(ULP)뿐. "100부터 유효숫자 손실"은 오류 |
| ×100 스케일이 수치적으로 해로운 이유? | 100은 2의 거듭제곱이 아님 → ÷100이 부정확 → 왕복 `x*100/100 != x` 가 ~14.6% |
| 확률 수백 개 곱이 0이 되는 현상과 처방? | 언더플로(최솟값 ~4.9e-324 아래로 붕괴) → 로그 공간: log(pq)=log p+log q, 곱→합 |
| 로그 확률의 값역? | p∈[0,1] → log p∈(−∞,0] → NLL(음의 로그 가능도)∈[0,∞) |

### [[18 인공지능 유형과 데이터 — 인지 AI·복잡성 4종·정형 비정형|18 AI 유형과 데이터]]
| 앞면 | 뒷면 |
|---|---|
| 인지 AI의 정의? 📌 | **데이터의 통계적 학습** — 환경 데이터 인식 → 패턴 감지 → 결론·의사 결정 |
| 4가지 AI 유형? 📌 | **인지**(패턴) · **이해**(의미를 앎) · **제어**(물리, 로봇) · **의식**(내적 경험 — 수학적 정의 부재로 범위 밖) |
| ML의 핵심 수학 문제 한 문장? 📌 | 데이터셋의 **신호를 잡고 노이즈를 무시**하는 함수(입력→출력)에 **적합(fit)** 후, **새 데이터에서 검증** |
| 그 문제를 복잡하게 만드는 4가지? 📌 | ① 가설·피처(생성 함수 미지) ② 성능(ground truth 부재) ③ 크기(고차원) ④ 구조(비정형) |
| AI의 최대 잠재력 지점? | 인간이 놓치는 **피처 간 미세한 상호 작용** 포착 |
| 정형 vs 비정형 vs 반정형? | 이름 있는 필드 테이블 / 그 외 대부분 / 메타데이터만 구조화(이메일) |

### [[19 실제 vs 시뮬레이션 데이터 — 키-체중·조건부 층화·노이즈|19 실제 vs 시뮬레이션 데이터]]
| 앞면 | 뒷면 |
|---|---|
| 실제 vs 시뮬 데이터 한 줄 대비? 📌 | 실제 = 관찰 수집·**생성 함수 미지**(가설만)·노이즈 / 시뮬 = **아는 함수·분포에서 생성** — 데이터가 함수 기능 수행 |
| 그림 2-1 "패턴 없음"의 정체? 📌 | 비만 과다 포함(교란) → **Index 층화**하면 선형 드러남 — 조건화는 공정한 탐색(MD 1.3 접속) |
| 시뮬을 현실적으로 만드는 3단? | ① 입력을 현실적 분포에서 샘플링 ② 함수 대입 ③ **노이즈 추가**(노이즈 분포도 선택 — 정규 vs 균등) |
| weight=−314.5+7.07×height, 키 60? | **109.7파운드** — 노이즈 없으면 5,000점이 완벽한 직선 |
| "ML·딥러닝 활동의 대부분"은? 📌 | 데이터에서 **w를 학습** — w 2개든 수백만 개든 수학적 구조 동일(🔭 MA 3장) |
| 쌍봉 히스토그램의 신호? | **혼합 분포** — 남/녀 두 정규 분포의 가우스 혼합(🔭) |

### [[20 수학 모델 — 선형·비선형·시뮬레이션과 AI 3중 관계|20 수학 모델 — 선형/비선형·AI]]
| 앞면 | 뒷면 |
|---|---|
| 선형 종속성이 모델링하는 것? 📌 | **세상의 평평함** — 직선(1D)·평면(2D)·초평면(고차원); 절대 구부러지지 않음 |
| 선형 함수의 문법? 📌 | 피처 **그 자체로만**, **스칼라 곱+덧셈/뺄셈만** — 거듭제곱·루트·분모·sin/cos/exp/log 금지; f=w₀+Σwᵢxᵢ |
| w₀의 이름과 정체? | **편향 항(bias term)** — 퍼셉트론의 b와 동일; 선형 함수=퍼셉트론=선형결합 삼위일체 |
| 신경망 성공의 원천(책)? 📌 | **피처 사이의 비선형적 관계를 포착하는 능력** |
| ⚠️ "선형 종속성" 용어 충돌? 📌 | 모델: 출력이 피처에 선형 의존 ≠ 벡터: 서로 중복 정보(1.8, det=0) — 같은 한국어, 다른 개념 |
| AI ↔ 수학 모델·시뮬 3중 관계? 📌 | ① 시뮬→AI 학습 데이터 생성(위험 상황 가상 훈련) ② AI→시뮬 향상(파라미터·메시) ③ AI 자체=수학 모델·시뮬레이션(기계의 일=계산) |

### [[21 어디서 데이터를 얻는가 — 저장소·크롤링·유명 훈련 데이터셋|21 데이터 획득]]
| 앞면 | 뒷면 |
|---|---|
| 실데이터 획득의 장애물 3종? 📌 | 규제 · 개인 정보 · 기관 심의 위원회 — 통과해도 비공개 경향; 낙담 없이 계속 요청 |
| 레이블 데이터가 유용한 이유? 📌 | **가설 함수의 결과를 측정할 기준** 제공(ground truth 문제의 해법) |
| AlexNet / WaveNet의 훈련 데이터? | ImageNet(수백만 이미지·크라우드소싱) / VCTK(109명 44h)+영어·만다린 음성+MagnaTagATune(188태그) — 전부 비정형 |
| §2.7의 기억할 3가지? 📌 | 디지털 데이터 필요 · 얻기 어려울 수 있음 · 전 세계 디지털화 진행 중 |

### [[22 확률·통계 용어 지도 — 확률 변수·분포·기대값·공분산·마르코프|22 확률·통계 용어 지도]]
| 앞면 | 뒷면 |
|---|---|
| 확률 이론의 목표(책)? 📌 | 랜덤한 숫자·확률적 사건을 **결정론적 명제로** 만드는 것 |
| 확률 변수 vs 함수? 📌 | **결괏값의 무작위성 vs 확실성** — 보기 전까지 모름 → 가능성 정량화로 목표 교체 |
| 이산/연속 분포 함수 이름? | pmf(질량) / **pdf(밀도)** — 문맥으로 구분 |
| 분포(distribution)란? 📌 | 확률 변수 값들에 총 확률 1을 나눠주는 **배분** — "퍼져 있다"는 뜻 아님 |
| 주변화 공식? 📌 | p(x)=Σ_y p(x,y) — 결합에서 다른 변수 전부 합(표 가장자리라 '주변') |
| 주변(marginal)의 어원? 📌 | 결합 확률 **표의 여백**에 행·열 합계를 적던 분할표 관행(칼 피어슨~1900) |
| 결합 분포를 전부 알면 베이즈는? 📌 | **불필요** — 베이즈는 결합을 모를 때의 도구(1.3 통찰의 책 확인) |
| 사전/사후/가능도의 조건 대상? 📌 | 관찰 전 가중치 / 데이터 주어진 후 가중치 / **가중치 주어진 데이터 관찰 확률의 함수**(합=1 불필요) |
| 불확실성 4대 요소·목표? 📌 | 기대값·평균(평균) + 분산·표준 편차(퍼짐) — **분산 제어로 불확실성 축소** |
| 기대값=표본 평균의 근거? | **큰 수의 법칙**(표본 무한대 = 전체 인구 측정의 이상적 경우) |
| 공분산 행렬 구조? 📌 | 대각=각 변수의 **분산** · 비대각=쌍의 공분산 · **대칭** · 대각화로 상관 제거(🔭 고유값) |
| 독립 vs 공분산 0? 📌 | 독립이 **더 강함** — 공분산·상관은 선형 관계만(비선형 의존은 숨음) |
| 정규화·스케일링·표준화 공통 형식? 📌 | **빼고(이동) 나누기(스케일)** — 표준화(평균·표준편차)·0~1(최소·범위)·단위벡터(노름) |
| 마르코프 과정 5요소? | 상태·행동·전이 확률 행렬·전이 분포·보상 함수(강화 학습, 🔭 MA 11장) |

### [[23 연속·이산 분포와 결합 확률 밀도 함수 — pdf·pmf·사후 확률 (모델 불확실성의 기하학)|23 연속·이산·결합 확률밀도]]
| 앞면 | 뒷면 |
|---|---|
| 연속 분포에서 정확한 값의 확률은? 📌 | **0** — 대신 구간(179.95<x<180.05)의 **면적**(pdf 적분)으로 정량화 |
| 연속 vs 이산 — 분포 함수 이름? 📌 | 연속 → **확률 밀도 함수(pdf)** / 이산 → **확률 질량 함수(pmf)** |
| 결합 분포를 알면? / 못 구하면 왜? 📌 | 알면 주변·조건부 전부 공짜(도시 전체 조망) — 독립이면 곱으로 쉬움, **종속이면 차원의 저주**로 매우 어려움 |
| 결합 pdf를 x₁=a로 고정하면? 📌 | f(a,x₂)는 **사후 확률 f(x₂\|x₁=a)에 비례**(그림 2-7) — 정규화해야 진짜 조건부 확률 |
| 결합 분포 앎 vs 베이즈 정리? | 결합을 아는 건 **사치스러울 만큼 강력한 정보** — 대부분은 **베이즈+사전분포+가능도**로 같은 사후분포 도달 |
| 곱셈 규칙으로 결합분포 학습 → 생성 모델 예시? | 딥마인드 **WaveNet**(오디오 생성) — 결합을 조건부 확률의 곱으로 분해해 학습 후 샘플링 |

### [[24 균등 분포·정규 분포 — 68-95-99.7·이변량 (가중치 초기화와 노이즈 모델)|24 균등·정규 분포]]
| 앞면 | 뒷면 |
|---|---|
| 균등 분포 pdf 공식? 📌 | f(x)=1/(x_max−x_min) (구간 내), 구간 밖 0 — **상수 함수** |
| 정규 분포 단변량 pdf 공식? 📌 | g(x;μ,σ)=1/√(2πσ²) · exp(−(x−μ)²/2σ²) |
| 68-95-99.7 정밀값? 📌 | **68.27% / 95.45% / 99.73%** (평균 ±1·2·3σ) — σ 크기 무관 |
| 표준정규 피크값? | 1/√(2π)=0.3989 (x=μ=0에서) |
| 이변량정규 Σ 행렬 구조? 📌 | 대각=σ1²,σ2²(각 분산), 비대각=ρσ1σ2(공분산) — **대칭** |
| "모르면 정규 분포"의 근거? 📌 | 동일 분산 조건에서 정규 분포가 **엔트로피(불확실성) 최대** — 가정을 가장 적게 인코딩 |
| 정규 분포와 CLT의 관계? 📌 | 동일 분포(정규 아니어도 됨) 독립 변수 **다수의 평균**이 정규로 수렴 → 정규가 자연계에 흔한 이유 |
| 그림 2-11의 등고선이 보여주는 것? | 6,000개 샘플이 정규 분포의 **등고선(타원)** 형태를 따름 — 분포를 모르고 봐도 정규로 추정 가능 |

### [[25 확률 분포 카탈로그 — 지수족·생존분석·기타 (대기시간과 이상치 모델링)|25 분포 카탈로그(13종)]]
| 앞면 | 뒷면 |
|---|---|
| 지수 분포의 고유 성질? 📌 | **무기억성** — 지금까지 기다린 시간과 무관하게 남은 대기시간 분포가 항상 같음 |
| 베이불 모양 파라미터=1이면? 📌 | **지수 분포와 완전히 동일**(검증됨) — 지수는 베이불의 특수형 |
| 감마(n,λ)의 정체는? 📌 | **지수(λ) n개의 합**의 분포 — n=1이면 지수 그 자체 |
| 카이제곱(k)의 정체는? 📌 | **표준정규 k개의 제곱합**의 분포 — 분산 계산·검정에 사용 |
| 로그정규 vs 정규(CLT) — 무엇의 극한? | 정규 = 독립 샘플의 **합**의 극한(CLT) / 로그정규 = 독립 샘플의 **곱**의 극한 |
| 파레토 vs 지수 — 꼬리? 📌 | 파레토가 **훨씬 두꺼운 꼬리**(느리게 감소) — x=20에서 지수보다 수천 배 큰 초과확률 |
| 이산 성공-시행 4형제 구분 축은? 📌 | (독립 vs 종속) × (성공횟수를 셈 vs 시행횟수를 셈) — 이항·기하/음이항·초기하/음의초기하 |
| 초기하 분포가 이항과 다른 점? | 시행이 **독립이 아님**(비복원 추출) — 나머지 구조는 이항과 유사 |
| 코시 분포의 특이점? | **평균·분산이 정의되지 않음** — 임의 값에 탄젠트를 적용해 생성 |
| '분포'의 4가지 의미(§2.14)? 📌 | 경험적 분포(표본) vs 근본적 분포(모집단, 미지) / 이산=pmf·CDF / 연속=pdf·CDF / 다변수=**결합 분포** |

### [[26 A·B 테스트 — 온라인 실험과 모델 롤아웃 검증|26 A/B 테스트]]
| 앞면 | 뒷면 |
|---|---|
| A/B 테스트의 다른 이름? 📌 | **분할 테스트(split test)** — 무작위 단일/이중 맹검 시험이라고도 함 |
| A/B 테스트 기본 절차? 📌 | 인구를 두 그룹으로 분할 → 한쪽에 새 버전 배포, 한쪽은 **대조군** 유지 → 결과 비교 |
| 단일 맹검 vs 이중 맹검? 📌 | 단일=**피험자만** 그룹 모름(실험자는 앎) / 이중=**피험자·실험자 둘 다** 모름 |
| 카나리 배포 = ? | A/B 테스트의 ML 인프라 버전 — 신모델을 트래픽 소수(1~5%)에만 먼저 노출 |
| A/B 테스트의 흔한 함정 2가지? 📌 | **표본비 불일치**(의도한 분할 비율과 실제가 어긋남=버그 신호) / **신기 효과**(새로워서 잠깐 반응, 며칠 뒤 원복) |

---

## 2단계 — 최적화와 역전파 (진행 시작)

### [[01 확률 — 정의·가능도·오즈 (불확실성 정량화)|01 확률 — 정의·가능도·오즈]] (원자 [[1.1 확률 — 정의·가능도·오즈|1.1]])
| 앞면 | 뒷면 |
|---|---|
| 확률의 정의? | 사건이 일어날 것이라고 **믿는 정도** — P(X), 작업은 0.0~1.0 소수 |
| 확률의 엄밀한 범위? 📌 | [0,1] (0%~100%) — 백분율/소수는 같은 값의 표시 형식 (근거는 T09: 비율·공리·곱셈 닫힘) |
| P(not X)? | 1 − P(X) — 0.70 → 0.30 |
| 확률 vs 가능도 차이 ①? 📌 | 확률 = *아직 안 일어난* 사건 예측의 정량화(미래) / 가능도 = *이미 발생한* 사건의 빈도 측정(과거) |
| 확률 vs 가능도 차이 ②? 📌 | 상호 배타적 결과들의 **확률 총합 = 1.0**; **가능도엔 이 규칙 없음**(경쟁 후보들의 점수) |
| 통계·ML에서 둘의 관계? 📌 | **확률(미래)을 예측하기 위해 데이터 형태에 대한 가능도(과거)를 사용** |
| 오즈 → 확률 공식? 📌 | P(X)=O(X)/(1+O(X)) — 7/3 → 0.7 |
| 확률 → 오즈 공식? 📌 | O(X)=P(X)/(1−P(X)) — 0.70 → 7/3; 두 공식은 **서로 역함수**(하나만 외우면 됨) |
| 오즈 2.0의 뜻? | 일어날 확률이 일어나지 않을 확률보다 **2배** 높음 |
| 오즈의 세 표기? | 7:3(비율) = 7/3(분수) = 2.333…(소수) — 전부 같은 값 |
| 확률·오즈·로그오즈 값역? | P∈[0,1] · O∈[0,∞) · log O∈(−∞,∞) — 1:1 단조 변환(정보 동일); 실수축 전체를 덮는 건 로그 오즈뿐(🔭 6장) |
| 오즈가 유용한 곳? | 주관적 믿음 정량화(도박·베팅) · 로그 오즈 로지스틱 회귀(🔭 6장) · 베이즈 요인/베이즈 통계(🔭) |
| 확률 vs 통계(분야)? 📌 | 확률 = 순전히 이론적 개념(**데이터 불요**) / 통계학 = **데이터 없이 존재 불가** — 데이터로 확률을 발견하고 데이터를 설명 |
| 주사위 P(4)의 두 접근? | 확률 사고: "6면 동일 *가정* → 1/6" 즉답 / 통계학자: "30번+ 굴려 데이터로 결정" — 공평하지 않다면 데이터 수집이 유일한 방법(🔭 3장) |

### [[02 확률 계산 — 주변·결합·합·덧셈 법칙 (AND·OR 확률 연산)|02 확률 계산 — 주변·결합·합·덧셈 법칙]] (원자 [[1.2 확률 계산 — 주변·결합·합·덧셈 법칙|1.2]])
| 앞면 | 뒷면 |
|---|---|
| 주변 확률? | 사건 P(X)의 **단일** 확률 — 결합 전 기본형 |
| 주변(marginal)의 어원? 📌 | 결합 확률 표를 행×열로 그릴 때 행·열 합을 표 **여백**에 적던 분할표 관행(칼 피어슨~1900) — 공간적 은유 아님 |
| 결합 확률 = 무슨 연산? 공식? 📌 | **AND** — P(A AND B)=P(A)×P(B) (독립 가정; 곱셈 정리) |
| P(앞면 AND 6)? | 1/2×1/6=1/12=0.08333… — 12조합 중 H6 1개 |
| 곱셈 정리의 근거? | **전 조합 열거·세기**(순열·조합 유래) — 곱셈은 지름길, 격자 면적 비율 |
| 상호 배타 사건의 OR? | 단순 합 — P(4 OR 6)=1/6+1/6=1/3 |
| 비배타 OR에 단순 합을 쓰면? 📌 | **교집합 이중 계산**(H6·T6의 6을 두 번) — 0.666 오답; 극단 1.333 = **공리 P(Ω)=1 위반 신호** |
| 확률의 덧셈 법칙? 📌 | P(A OR B)=P(A)+P(B)−P(A AND B) — 결합 사건을 한 번만 |
| P(앞면 OR 6)? | 1/2+1/6−1/12=7/12=0.58333…(열거 7/12와 일치) |
| 덧셈 법칙, 배타 사건에선? | P(A AND B)=0 → 단순 합으로 환원 — **일반형 하나가 전 경우 포괄** |
| −P(A)×P(B)형은 언제만? ⚠️📌 | **독립일 때만** — 배타(≠독립)에 쓰면 P(4 OR 6)=11/36≠1/3 오답 |
| 독립 vs 상호 배타? 📌 | 독립 = 서로 정보 없음(P(A∩B)=P(A)P(B)) / 배타 = 함께 불가(P(A∩B)=0) — 비영 확률이면 양립 불가 |
| 곱셈 정리의 일반형(예고)? | P(A AND B)=P(A)×P(B\|A) — 조건부 확률(→ 03에서 회수 완료) |

### [[03 조건부 확률·베이즈 정리 — 방향·뒤집기·조건부 결합·합 (다음 토큰 확률)|03 조건부 확률·베이즈 정리]] (원자 [[1.3 조건부 확률·베이즈 정리 — 방향·뒤집기·조건부 결합·합|1.3]])
| 앞면 | 뒷면 |
|---|---|
| 조건부 확률 P(A\|B)? | **B가 발생했을 때** A가 발생할 확률 — P(A GIVEN B); 확률에서 가장 헷갈리는 개념 |
| 조건의 방향이 중요한 이유? 📌 | P(커피\|암)=85% ≠ P(암\|커피)=0.65% — 같은 교집합(0.00425)을 **다른 분모**(암 0.5% vs 커피 65%)로 나눈 것 |
| 베이즈 정리? 📌 | P(A\|B) = P(B\|A)P(A) / P(B) — 조건부 확률의 방향 뒤집기 |
| 베이즈 정리의 유래? | 결합의 두 방향 분해 동일성 P(A\|B)P(B)=P(B\|A)P(A)를 P(B)로 나눈 재배열 — 새 공리 아님 |
| 커피-암 뒤집기 계산? | 0.85×0.005/0.65 = 17/2600 ≈ 0.0065 (0.65%) |
| 조건부 결합(일반 곱셈 법칙)? 📌 | P(A AND B) = P(A\|B)×P(B) — **종속 여부 무관**; 독립이면 P(A)P(B)로 축소 |
| 옵션 1 vs 옵션 2 (커피 AND 암)? | P(커피)P(암)=0.00325(조건부 없을 때 최선) vs P(커피\|암)P(암)=0.00425(정답 — 더 구체적 조건 사용) |
| 조건부 합(덧셈 법칙 일반형)? 📌 | P(A OR B) = P(A)+P(B)−P(A\|B)×P(B) — 배타면 P(A\|B)=0 → 단순 합 자동 환원 |
| 독립의 정식 정의? 📌 | P(A\|B) = P(A) — B가 일어나도 A의 가능성 불변 (1.2 "가정"의 실체) |
| "0.0065×0.65=0.00425"의 함정? ⚠️ | 반올림 값으론 0.004225 — 등식은 **정확값**(17/2600 × 13/20 = 17/4000)에서만 성립 |
| 조건부 확률 읽기 3단계? | ① 방향 확인 ② **기저율과 비교**(리프트 17/13≈1.31) ③ 데이터 자격 심문(정의·출처, 🔭 3장) |
| 공식 계보(결합)? | 열거(진리) → P(A)P(B)(독립 특수형, 1.2) → P(A\|B)P(B)(일반형, 1.3) — 독립은 특수 사례로 강등 |
| LLM 다음 토큰 확률의 정체? | 조건부 확률 P(wₜ\|w₁…wₜ₋₁) — 시퀀스 결합 = 조건부 연쇄(어순 보존; 독립 곱은 어순 소실) |

### [[04 이항 분포·베타 분포 — pmf·CDF·확률의 확률 (평가 지표의 불확실성)|04 이항 분포·베타 분포]] (원자 [[1.4 이항 분포·베타 분포 — pmf·CDF·확률의 확률|1.4]])
| 앞면 | 뒷면 |
|---|---|
| 이항 분포의 정의? 📌 | 확률이 p일 때 **n번 시도 중 k번 성공**할 가능성 — `binom.pmf(k, n, p)` |
| 이항 pmf의 부품(공식)? | C(n,k)·p^k·(1−p)^(n−k) — 독립 AND 곱(1.2 곱셈 정리) × 배열 수(경우의 수) |
| 엔진 테스트(p=0.9)의 교훈 수치? 📌 | P(k≤8)=0.2639 — 기본 성공률 90%여도 8회 이하가 26.39% → "동전 10번 앞면 8개 ≠ 동전 확률 80%" |
| n=10, p=0.9의 최빈 성공 횟수? | 9회(0.3874) — 8회는 0.1937, 10회는 0.3487 |
| 베타 분포의 정의? 📌 | **α번 성공·β번 실패**가 주어졌을 때 사건의 **다양한 기본 확률의 가능성** — 확률의 확률(메타 확률) |
| 베타의 x축·y축? | x = 기본 성공률 0.0~1.0 / y = 그 성공률의 **가능도(밀돗값 ≠ 확률)** |
| 연속 분포에서 확률을 얻는 법? 📌 | **곡선 아래 면적** — 전체 면적 = 1.0(확률 분포의 자격) |
| CDF의 정의와 방향? 📌 | 주어진 x까지의 **왼쪽** 면적 — 이상(오른쪽)=1−CDF, 중간=CDF(b)−CDF(a) |
| 8/10에서 P(p≥0.9)? | 1 − beta.cdf(0.9, 8, 2) = 0.225 (22.5%) — 90% 이하일 가능성 77.48% |
| 30/36으로 갱신하면? 📌 | 분포가 **좁아지고** P(p≥0.9): 22.5% → **13.16%** — 데이터 추가 = 불확실성 압축(믿음 업데이트) |
| 중간 영역 0.8~0.9? | cdf(0.9) − cdf(0.8) = 0.3386 (33.86%) |
| ⚠️ Beta(8,2)의 정점은? 📌 | (a−1)/(a+b−2) = **0.875** — 관측률 0.8이 아니다; 0.8에 정점을 두려면 Beta(s+1,f+1)=Beta(9,3) |
| 이항 vs 베타 — 한 줄 대비? 📌 | 이항 = p 고정, k의 분포(확률 방향·미래) / 베타 = 관측 고정, p의 분포(가능도 방향·과거) — 1.1 구분의 실체화 |

---

## 0단계 — PyTorch 기초 (닻 올리기)

### [[01 PyTorch 설치 트러블슈팅 (Apple Silicon)|01 설치 트러블슈팅]]
| 앞면 | 뒷면 |
|---|---|
| Apple Silicon GPU 확인 API? | `torch.backends.mps.is_available()` (MPS=Metal) |
| NVIDIA GPU 확인 API? | `torch.cuda.is_available()` |
| "설치됐는데 import 실패" 원인? | pip와 python이 서로 다른 파이썬 → venv로 짝 일치 |
| torch 2.4.0 호환 사슬? | 파이썬 3.12, numpy 1.x (`"numpy<2.0.0"`) |

### [[02 텐서 이해하기|02 텐서]]
| 앞면 | 뒷면 |
|---|---|
| 텐서 차수(rank/order)? | 차원 개수. 스칼라0·벡터1·행렬2·nD |
| PyTorch 텐서 = NumPy + 무엇 2가지? | ① autograd ② GPU 가속 |
| dtype 기본(정수/실수)? | int64 / float32 |
| 왜 float32 기본? | 메모리 적고 빠름·GPU 최적화·딥러닝엔 충분 |
| `.reshape` vs `.view`? | view=연속 메모리 필요 / reshape=무관(필요시 복사) |
| 행렬 곱 연산자? | `.matmul()` 또는 `@` |

### [[03 계산 그래프|03 계산 그래프]]
| 앞면 | 뒷면 |
|---|---|
| 계산 그래프란? | 수식을 표현하는 유향 그래프 (역전파 토대) |
| autograd란? | 동적 그래프에서 그레이디언트 자동 계산 엔진 |
| 동적 그래프(Define-by-Run)? | 코드 실행과 동시에 그래프 실시간 빌드 |
| 그래프는 언제? | 텐서 중 하나라도 `requires_grad=True`일 때 |
| `grad_fn`? | "어떤 연산으로 태어났나" 기록 → 역추적 |

### [[04 자동 미분 (autograd)|04 autograd]]
| 앞면 | 뒷면 |
|---|---|
| 역전파의 수학 원리? | 연쇄 법칙 = 후진 모드 자동 미분 (loss→입력) |
| autograd 핵심 3단계? | ① requires_grad=True ② loss.backward() ③ .grad 확인 |
| `loss.backward()`가 하는 일? | 모든 leaf의 .grad에 ∂L/∂param 저장 |
| `grad()` vs `backward()`? | 원하는 것만(디버깅) vs 모든 leaf(실전); 결과 동일 |
| `retain_graph=True`? | 기본은 계산 후 그래프 삭제 → 재사용 시 유지 |

### [[05 퍼셉트론·편향·은닉층|05 퍼셉트론·은닉층]]
| 앞면 | 뒷면 |
|---|---|
| 퍼셉트론? | 입력×가중치 합이 임계값 넘으면 1 (기본 스위치) |
| 가중치 vs 편향? | 가중치=입력 비례 곱(2D) / 편향=입력 무관 오프셋(1D) |
| `nn.Linear` 연산? | W·x + b (완전연결층) |
| ReLU 정의? | max(0, x) (비선형성) |
| 선형 붕괴(Linear Collapse)? | 비선형 없이 선형층만 쌓으면 한 층으로 합쳐짐 |
| 로짓→확률? | `torch.softmax(out, dim=1)` |
| Addmm 뜻? | 행렬곱(mm)+덧셈(add)=Wx+b (nn.Linear grad_fn) |

### [[06 가중치 — 상태·초기화·학습 실패|06 가중치·초기화]]
| 앞면 | 뒷면 |
|---|---|
| 가중치(Weight)? | 입력 변형 연결 강도·학습으로 새겨진 상태 |
| `requires_grad` 의미? | 학습 모드: True=훈련 / False=프리징 |
| 0으로 초기화하면? | 대칭성 안 깨져 모든 뉴런 동일 갱신 → 학습 실패 |
| 대칭성 깨기? | 작은 난수 초기화 (`manual_seed`로 재현성) |
| grad 소실 vs 폭주? | 0에 잠김 / NaN·Inf로 터짐 |
| Xavier·He 초기화? | 층 크기에 맞춰 분산 조절 (소실·폭주 완화) |

### [[07 데이터 로더 (Dataset · DataLoader)|07 데이터 로더]]
| 앞면 | 뒷면 |
|---|---|
| Dataset 3 메서드? | `__init__` · `__getitem__(idx)` · `__len__` |
| Dataset vs DataLoader? | 한 건 꺼내는 법 / 배치·셔플·병렬 공급 |
| DataLoader 핵심 인자 4? | batch_size · shuffle · drop_last · num_workers |
| 에포크 vs 이터레이션? | 전체 1회 순회 / 배치 1개=가중치 1회 갱신 |
| 테스트 로더 설정? | shuffle=False, drop_last=False |
| 레이블 규칙? | 0부터 시작, 최대 ≤ 출력 노드 수 − 1 |

### [[08 훈련 루프 (Training Loop)|08 훈련 루프]]
| 앞면 | 뒷면 |
|---|---|
| 훈련 루프 5단계? | forward(logits) → loss → zero_grad → backward → step 📌 |
| `zero_grad()`를 왜 매번? | grad가 누적됨 → 안 비우면 이전 배치 grad가 쌓임 |
| `F.cross_entropy` 특징? | 로짓을 직접 받아 내부 softmax+NLL (마지막에 softmax 안 붙임) |
| `optimizer.step()`? | grad로 파라미터 갱신 (SGD: param −= lr·grad) |
| train() / eval()? | 드롭아웃·BN 동작이 다른 층 위한 모드 전환 |
| 정확도 공식? | 맞은 예측 / 전체 |
| 검증셋 vs 테스트셋? | 튜닝(여러 번) / 평가 한 번 |

### [[09 모델 저장과 로드 (state_dict)|09 저장·로드]]
| 앞면 | 뒷면 |
|---|---|
| 모델 저장(추천)? | `torch.save(model.state_dict(), "m.pth")` (상태만) |
| state_dict란? | 층↔파라미터(가중치·편향) 매핑 딕셔너리 |
| 로드 2단계? | ① 구조 먼저 생성 ② `load_state_dict(torch.load(...))` |
| 로드 필수 조건? | 구조가 원본과 정확히 일치 |
| 왜 전체 대신 state_dict? | 전체=pickle 클래스 의존(이식성↓) / state_dict=순수 텐서(이식성↑) |

### [[10 GPU 가속과 분산 훈련 (DDP)|10 GPU·DDP]]
| 앞면 | 뒷면 |
|---|---|
| 장치(device)? | 텐서가 놓이고 연산하는 곳 (cpu·cuda·mps) |
| 같은 장치 규칙? | 연산 참여 텐서가 모두 동일 device (아니면 RuntimeError) |
| 단일 GPU 3줄? | device 정의 → model.to(device) → 데이터.to(device) |
| 디바이스-애그노스틱? | `cuda if is_available() else cpu` (맥은 mps) |
| DDP란? | 모델 복제 + 데이터 분할 → 각자 forward/backward → grad all-reduce |
| 데이터 병렬 vs 모델 병렬? | 미니배치 나눔(DDP) / 모델 나눔(FSDP·ZeRO) |
| rank / world_size? | 프로세스 ID(GPU ID) / 총 프로세스 수 |

### [[T01 경사 하강법과 훈련 루프|탐구 T01 — 경사 하강법]]
| 앞면 | 뒷면 |
|---|---|
| 학습의 실체? | grad 반대로 가중치 반복 갱신해 손실 최소화 |
| SGD 갱신 공식? | w ← w − lr·grad |
| 학습률(lr)? | 보폭; 크면 발산, 작으면 느림 |
| 에폭 vs 스텝? | 스텝=1회 갱신 / 에폭=전체 1회 통과 |
| 경사 하강 함정 4? | 지역 최솟값·안장점·평탄 구역·오버슈팅 |
| 함정 극복 → 옵티마이저? | 관성+적응 학습률 → Adam |

### [[T03 PyTorch 데이터 로딩 성능 최적화|탐구 T03 — 데이터 로딩 성능]]
| 앞면 | 뒷면 |
|---|---|
| 로딩 병목 숨기는 3옵션? | num_workers>0 + prefetch_factor + persistent_workers |
| pin_memory=True 효과? | page-lock RAM → DMA로 GPU 비동기 고속 복사 |
| 무거운 전처리는 어디서? | `__init__`에서 미리 (not `__getitem__`) |
| non_blocking=True? | .to(device) 비동기 — 전송·연산 겹치기 |
| macOS num_workers 에러? | set_sharing_strategy('file_system') |

---

## 통찰 (cross-domain)

### [[두 전파 — 역전파 vs 라우팅 (그래프는 같고 메시지는 다르다)|두 전파]]
| 앞면 | 뒷면 |
|---|---|
| 역전파 vs 라우팅 — 같은 것? | 위상(그래프)·메시지 전달 양식뿐 |
| 전파 대상? | 그래디언트(오차) vs 경로 정보(거리) |
| 수학? | 연쇄법칙·자동미분 vs Bellman-Ford·Dijkstra |
| MoE 라우터는 역전파 대체? | 아님 — 게이팅도 역전파로 학습(미분 가능 라우팅) |

### [[왜 선형대수인가 — 데이터·알고리즘의 표준 언어|왜 선형대수인가]]
| 앞면 | 뒷면 |
|---|---|
| 선형 변환 두 조건? | 가법성 f(a+b)=f(a)+f(b) · 동차성 f(cx)=c·f(x) |
| 피보나치 O(log N) 도구? | 상태 전이 행렬 + 거듭제곱 분할정복 |
| 인접 행렬 (Aᵏ)ᵢⱼ? | i→j 길이 k walk 수 |
| PageRank 본질? | 링크 행렬의 지배 고유벡터 |

### [[벡터 — 기하·물리·선형대수를 관통하는 한 뿌리 (피타고라스·삼각함수)|벡터 한 뿌리]]
| 앞면 | 뒷면 |
|---|---|
| 노름의 뿌리? | 피타고라스 √(x₁²+…+xₙ²) — 벡터(1.2)·행렬(11) 노름 공통 뿌리 |
| 내적 기하 정의? | A·B=‖A‖‖B‖cosθ |
| 코사인 유사도? | (A·B)/(‖A‖‖B‖) — 방향(의미)만 |
| 벡터 분해/합성? | Fx=Fcosθ, Fy=Fsinθ / F=√(Fx²+Fy²) |

### [[정적 vs 동적 — 컴파일타임과 런타임이 가르는 것들|정적 vs 동적]]
| 앞면 | 뒷면 |
|---|---|
| 정적 vs 동적 절대 기준? | 컴파일타임에 박힘 vs 런타임에 결정 |
| Late Binding? | 값·연결을 실행 중 늦게 결정 |
| ML 대응? | 지도학습/LLM=정적, 강화학습=동적 |
| 정적/동적 라이브러리? | `.a`(embed) vs `.so`(런타임 링킹) |

### [[엔트로피와 질서 — 최소 에너지 법칙과 엔지니어링|엔트로피와 질서]]
| 앞면 | 뒷면 |
|---|---|
| 열역학 제2법칙? | 닫힌 계 엔트로피 증가; 질서엔 외부 에너지 필요 |
| 소프트웨어 엔트로피? | 외부 에너지 없으면 코드는 무질서(스파게티)로 |
| System 1 vs 2? | 직관·저에너지(S1) vs 추상·고에너지(S2) |

---

## 주제 (AI-ML 독립)

### [[AI 아첨 편향(Sycophancy) — 원인과 깨는 법 (프롬프트·시스템)|AI 아첨 편향]]
| 앞면 | 뒷면 |
|---|---|
| 아첨 편향(Sycophancy)? | 사용자 의견·유도에 사실 무관하게 동조하는 편향 |
| 근본 원인? | RLHF의 불완전한 선호 (동조 답을 정답보다 선호) |
| 스케일·instruction tuning 영향? | 줄이는 게 아니라 오히려 증가 |
| "Ask, Don't Tell"? | 정답·신념 암시 없이 중립 질문으로 묻기 |

### [[LLM은 왜 길어질수록 평이해지나 — 어텐션 희석과 엔트로피|LLM 평이화]]
| 앞면 | 뒷면 |
|---|---|
| 다음 토큰 베이즈 비례식? | P(Token\|Context) ∝ P(Context\|Token)·P(Token) |
| 베이지안 붕괴? | 우도 무력화 → 사후가 Prior에만 지배 |
| 섀넌 엔트로피? | H(X)=−Σ P(xᵢ)log₂P(xᵢ) (평평할수록 큼) |
| Cross-Entropy Loss? | 정답에 확률 몰아 엔트로피 최소화 |

### [[강화학습 패러다임 — 정적 데이터 vs 실시간 상호작용|강화학습 패러다임]]
| 앞면 | 뒷면 |
|---|---|
| RL 학습 신호? | label(정답)이 아니라 reward(보상) |
| ML 세 패러다임? | 비지도·지도·강화 |
| RL 반복 단위? | 에포크 아닌 에피소드(게임 1판) |
| 점수판 갱신 수식? | 벨만 방정식 |
| 정책(Policy)? | 상태 → 행동을 고르는 규칙 |

---

## CS

### [[GPU 하드웨어 구조 — SIMT·SM·텐서코어 (ARM 관점)|GPU 하드웨어]]
| 앞면 | 뒷면 |
|---|---|
| SIMT? | 제어부 1개가 같은 명령을 수천 스레드에 브로드캐스트 |
| CPU vs GPU 철학? | 지연 최소화(제어·캐시) vs 처리량 극대화(ALU 수천) |
| GPU 계층? | SM ⊃ CUDA코어·텐서코어 + Register·Shared Mem + VRAM |
| 텐서 코어? | 타일 행렬곱(MMA)을 한 명령으로 처리 |
| 분기 발산? | if/else로 스레드 갈려 순차 실행 → 효율 반토막 |
| GEMM? | General Matrix Multiply (Y=WX+b) |

### [[IPC — 프로세스 간 통신 (공유 메모리·소켓·OS별)|IPC]]
| 앞면 | 뒷면 |
|---|---|
| IPC? | 격리된 프로세스가 데이터를 주고받는 방법 통칭 |
| IPC 두 방식? | 공유 메모리(빠름·동기화 필요) / 메시지 패싱(안전·직렬화 느림) |
| 로컬: IPC vs TCP/IP? | IPC가 수~수십배 빠름 (네트워크 스택 바이패스) |
| fork vs spawn? | 부모 메모리 복사(빠름) / 새 인터프리터(안정·느림) |

### [[메모리·캐시·멀티프로세스 (시스템 원리)|메모리·캐시·멀티프로세스]]
| 앞면 | 뒷면 |
|---|---|
| 메모리 계층 순서? | 디스크 ≪ RAM ≪ 캐시(L3<L2<L1) ≪ 레지스터 |
| 캐시 라인? | RAM에서 한 번에 가져오는 최소 단위(보통 64B) |
| 캐시 히트율 ↑? | 연속 메모리·작은 dtype·지역성·벡터화 |
| False Sharing? | 다른 코어가 같은 캐시라인 인접 데이터 수정 → 무효화 |
| GIL? | 전역 인터프리터 락; 스레드 진짜 병렬 막음 → 멀티프로세스 우회 |
| DMA? | CPU 개입 없이 장치↔RAM 직접 전송 (page-lock 필요) |

---

## 개발 · 수학

### [[파이썬 OOP — self · super() · 매직 메서드|파이썬 OOP]]
| 앞면 | 뒷면 |
|---|---|
| `self`? | 인스턴스 메서드 첫 파라미터 = 객체 자기 자신 |
| 부모 클래스 접근? | `super()` — `super().__init__()` |
| `self` 자동 주입 원리? | `obj.m(x)` → `Class.m(obj, x)` |
| `len(obj)` 발동? | `__len__(self)` |
| `obj[i]` 발동? | `__getitem__(self, i)` |
| 덕 타이핑? | 약속된 메서드만 있으면 타입 무관 동작 |

### [[3D 변환 행렬 — 동차좌표·회전·투영 (선형대수 응용)|3D 변환 행렬]]
| 앞면 | 뒷면 |
|---|---|
| 동차좌표? | (x,y,z)→(x,y,z,1) → 이동까지 4×4 행렬곱에 통합 |
| w=1 vs w=0? | 점(위치, 이동 받음) / 벡터(방향, 안 받음) |
| 4대 변환? | 이동·크기·회전 (전부 4×4 행렬) |
| 복합 변환? | 행렬들을 미리 곱해 단일 행렬로 (결합법칙) |
| 회전행렬은 좌표를? | 점이 아니라 좌표계 자체를 회전시키는 연산자 |
| 2D 회전 행렬 재구성법? | 열 읽기: 1열=x̂의 상 (cosθ,sinθ) / 2열=ŷ의 상 (−sinθ,cosθ) |
| 2D 총람 det 3분류? | +1(항등·이동·회전·전단·원점 반사) / WH(스케일) / −1(축 반사=거울) |
| 원점 반사의 정체? | det=+1 — 거울 아닌 **180° 회전**(축 반사만 진짜 거울) |
| 전단의 고유벡터? | 전단 x는 x축 [1,0]ᵀ(λ=1) — 유일하게 제자리 |
| 회전의 실고유벡터? | 없음(0<θ<180°) — 복소 고윳값(12장 예고) |
| 임의 점 p 기준 회전? | 합성 TₚRT₋ₚ — 동차좌표 덕에 단일 행렬로 접힘 |

### [[Docker]] · [[Prisma]]
| 앞면 | 뒷면 |
|---|---|
| Docker 컨테이너 재시작? | `docker compose down && docker compose up -d` |
| 컨테이너 로그? | `docker logs <이름>` |
| Prisma란? | Node.js ORM; 스키마 동기화·마이그레이션 |
| 기존 DB → schema.prisma? | `npx prisma db pull` |
| Prisma Client 생성? | `npx prisma generate` |

---

> 🔄 갱신 이력: 전 노트(35) 전수 조사로 초기 구축. 새 노트의 암기 카드는 해당 영역 표에 한 줄씩 추가.
